import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.57.4';
const URL='https://uqlolefsiktbznnymriy.supabase.co',KEY='sb_publishable_3sUsinfJ_nMFI44OXozkKQ_jmGG8w7n',REM='komo_pulse_remember',SEL='komo_patient_message_record';
const S={client:null,session:null,role:'member',records:[],selected:null,thread:null,messages:[],loading:false,error:''};
function storage(){return localStorage.getItem(REM)==='1'?localStorage:sessionStorage}function sb(){if(!S.client)S.client=createClient(URL,KEY,{auth:{storage:storage(),persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});return S.client}function esc(v=''){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function fmt(v){if(!v)return'—';return new Intl.DateTimeFormat('fr-FR',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(v))}function route(){return location.hash.replace(/^#/,'')||'home'}
function memberMode(){if(S.role==='member')return true;return !!document.querySelector('#modeSwitch [data-mode="member"].active')}function centerName(r){return r?.organizations?.name||'Centre KŌMØ'}
const icon='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M4 5h16v12H9l-5 4z"/><path d="M8 9h8M8 13h5"/></svg>';
async function base(){const{data:{session}}=await sb().auth.getSession();S.session=session;if(!session?.user)return false;const rr=await sb().from('account_roles').select('role').eq('user_id',session.user.id).maybeSingle();S.role=rr.data?.role||'member';return true}
function makeNav(host){if(!host||host.querySelector('[data-route="messages"]'))return;const b=document.createElement('button');b.type='button';b.className='nav-item';b.dataset.route='messages';b.setAttribute('aria-label','Messages');b.innerHTML=icon+'<span>Messages</span>';const explore=host.querySelector('[data-route="explore"]');host.insertBefore(b,explore||null);b.addEventListener('click',()=>{location.hash='messages'})}
function ensureNav(){makeNav(document.querySelector('#desktopNav'));makeNav(document.querySelector('#mobileNav'));document.querySelectorAll('#desktopNav [data-route="plan"],#mobileNav [data-route="plan"]').forEach(b=>{const x=b.querySelector('span');if(x&&x.textContent!=='Mes priorités')x.textContent='Mes priorités';b.setAttribute('aria-label','Mes priorités')})}
async function loadRecords(){const q=await sb().from('patients').select('id,organization_id,patient_user_id,first_name,last_name,preferred_name,email,external_reference,organizations(id,name,city)').eq('patient_user_id',S.session.user.id).order('updated_at',{ascending:false});if(q.error)throw q.error;S.records=q.data||[];const saved=localStorage.getItem(SEL);S.selected=S.records.find(x=>x.id===saved)||S.records[0]||null;if(S.selected)localStorage.setItem(SEL,S.selected.id)}
async function loadConversation(){S.thread=null;S.messages=[];if(!S.selected)return;const t=await sb().rpc('ensure_patient_message_thread',{p_patient_id:S.selected.id});if(t.error)throw t.error;S.thread=t.data;const q=await sb().from('messages').select('id,thread_id,sender_user_id,body,created_at').eq('thread_id',S.thread).order('created_at',{ascending:true}).limit(300);if(q.error)throw q.error;S.messages=q.data||[];await sb().from('message_thread_reads').upsert({thread_id:S.thread,user_id:S.session.user.id,last_read_at:new Date().toISOString()},{onConflict:'thread_id,user_id'})}
function bubble(m){const mine=m.sender_user_id===S.session?.user?.id;return`<div class="kmsg-bubble ${mine?'mine':'other'}"><div>${esc(m.body)}</div><small>${mine?'Vous':'Équipe KŌMØ'} · ${fmt(m.created_at)}</small></div>`}
function conversation(){if(S.loading)return'<div class="kmsg-empty">Chargement de la conversation…</div>';if(S.error)return`<div class="kmsg-empty"><strong>Messagerie indisponible.</strong><br>${esc(S.error)}</div>`;if(!S.selected)return'<div class="kmsg-empty">Votre messagerie s’active dès qu’un dossier est rattaché à un centre.</div>';return`<div class="kmsg-thread" data-kmsg2-thread>${S.messages.length?S.messages.map(bubble).join(''):'<div class="kmsg-empty">La conversation est ouverte. Envoyez votre premier message à l’équipe qui suit votre bilan.</div>'}</div><form class="kmsg-compose" id="kmsg2Compose"><textarea name="body" rows="2" maxlength="4000" placeholder="Écrire à mon équipe KŌMØ…" required></textarea><button class="primary-button" type="submit">Envoyer →</button></form>`}
function render(){if(route()!=='messages'||!memberMode())return;const root=document.querySelector('#viewRoot');if(!root)return;const e=document.querySelector('#pageEyebrow'),t=document.querySelector('#pageTitle');if(e)e.textContent='MESSAGES';if(t)t.textContent='Votre équipe KŌMØ.';document.querySelectorAll('#desktopNav .nav-item,#mobileNav .nav-item').forEach(x=>x.classList.toggle('active',x.dataset.route==='messages'));root.innerHTML=`<div class="kmsg-pro" data-kmsg2><section class="kmsg-head"><div><p class="eyebrow">KŌMØ MESSAGES</p><h2>Une conversation liée à votre suivi.</h2><p>Échangez avec le centre et les professionnels qui ont accès à votre dossier KŌMØ. Les messages restent rattachés à votre parcours.</p></div><div class="kmsg-count"><span>Centres liés</span><strong>${S.records.length}</strong></div></section>${S.records.length?`<section class="kmsg-layout"><aside class="kmsg-people"><div class="kmsg-people-title">Mes conversations</div>${S.records.map(r=>`<button data-kmsg2-record="${r.id}" class="${S.selected?.id===r.id?'active':''}"><strong>${esc(centerName(r))}</strong><span>${esc(r.organizations?.city||'Votre équipe')}</span><small>${esc(r.external_reference||'')}</small></button>`).join('')}</aside><article class="kmsg-conversation"><div class="kmsg-conv-head"><div><span>Conversation de suivi</span><strong>${esc(centerName(S.selected))}</strong><small>${esc(S.selected?.organizations?.city||'')}</small></div></div>${conversation()}</article></section>`:'<article class="card"><div class="empty-state"><strong>Aucune conversation active pour le moment.</strong><br>La messagerie devient disponible dès qu’un centre est associé à votre dossier Motion ou Clinical.</div></article>'}</div>`;bind();setTimeout(()=>{const x=root.querySelector('[data-kmsg2-thread]');if(x)x.scrollTop=x.scrollHeight},30)}
function bind(){document.querySelectorAll('[data-kmsg2-record]').forEach(b=>b.addEventListener('click',async()=>{S.selected=S.records.find(x=>x.id===b.dataset.kmsg2Record)||null;if(S.selected)localStorage.setItem(SEL,S.selected.id);S.loading=true;S.error='';render();try{await loadConversation()}catch(e){S.error=e.message||'Impossible de charger la conversation.'}finally{S.loading=false;render()}}));document.querySelector('#kmsg2Compose')?.addEventListener('submit',send)}
async function send(e){e.preventDefault();const form=e.currentTarget,body=String(new FormData(form).get('body')||'').trim();if(!body||!S.thread)return;const b=form.querySelector('button');b.disabled=true;try{const q=await sb().from('messages').insert({thread_id:S.thread,sender_user_id:S.session.user.id,body});if(q.error)throw q.error;form.reset();await loadConversation();render()}catch(err){S.error=err.message||'Message non envoyé.';render()}finally{b.disabled=false}}
async function open(force=false){ensureNav();if(route()!=='messages')return;if(!await base()||!memberMode())return;S.loading=true;S.error='';render();try{await loadRecords();await loadConversation()}catch(e){S.error=e.message||'Messagerie indisponible.'}finally{S.loading=false;render()}}
window.addEventListener('hashchange',()=>setTimeout(()=>open(false),70));window.addEventListener('komo:route-ready',()=>{ensureNav();if(route()==='messages')setTimeout(()=>open(false),50)});document.querySelector('#modeSwitch')?.addEventListener('click',()=>setTimeout(()=>open(false),100));document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>open(true),650));setTimeout(()=>{ensureNav();open(false)},1200);
