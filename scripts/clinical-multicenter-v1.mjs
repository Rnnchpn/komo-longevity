import { readFile, writeFile } from 'node:fs/promises';
const cockpitPath='pulse-app/clinical-cockpit-v1.js';
const indexPath='pulse-app/index.html';
let src=await readFile(cockpitPath,'utf8');
const replacements=[
  ["const K={patient:'komo_clinical_patient',assessment:'komo_clinical_assessment',tab:'komo_clinical_tab'};","const K={patient:'komo_clinical_patient',assessment:'komo_clinical_assessment',tab:'komo_clinical_tab',org:'komo_clinical_org'};"],
  ["const s={client:null,session:null,role:'member',org:null,patients:[],assessments:[],scores:[],imports:[],priorities:[],contexts:[],appointments:[],patient:null,assessment:null,tab:localStorage.getItem(K.tab)||'dashboard',loaded:false};","const s={client:null,session:null,role:'member',org:null,membership:null,memberships:[],patients:[],pulseAccounts:[],assessments:[],scores:[],imports:[],priorities:[],contexts:[],appointments:[],patient:null,assessment:null,tab:localStorage.getItem(K.tab)||'dashboard',loaded:false};"],
  [".select('organization_id,status,organizations(id,name,slug,clinical_data_status,status)')",".select('organization_id,role,access_scope,status,organizations(id,name,slug,clinical_data_status,status)')"],
  ["const memberships=om.data||[],m=memberships.find(x=>x.organizations?.slug==='komo-poc')||memberships[0];s.org=m?.organizations||null;","const memberships=om.data||[];s.memberships=memberships;if(s.role==='admin'){s.membership={role:'admin',access_scope:'clinical'};s.org={id:null,name:'Tous les centres',slug:'global-admin',clinical_data_status:'global'}}else{const savedOrg=localStorage.getItem(K.org),m=memberships.find(x=>x.organization_id===savedOrg)||memberships[0];s.membership=m||null;s.org=m?.organizations||null;if(s.org)localStorage.setItem(K.org,s.org.id)}"],
  ["const pr=await c.from('patients').select('*').eq('organization_id',s.org.id).order('created_at',{ascending:false});s.patients=pr.data||[];","let patientQuery=c.from('patients').select('*,organizations(id,name,slug,clinical_data_status)').order('created_at',{ascending:false});if(s.role!=='admin')patientQuery=patientQuery.eq('organization_id',s.org.id);const pr=await patientQuery;s.patients=pr.data||[];s.pulseAccounts=[];if(s.role==='admin'){const reg=await c.functions.invoke('admin-registry',{body:{action:'list'}});const linked=new Set(s.patients.map(p=>p.patient_user_id).filter(Boolean));s.pulseAccounts=(reg.data?.patients||[]).filter(x=>!linked.has(x.user_id))}"],
  ["const ap=await c.from('organization_appointments').select('*').eq('organization_id',s.org.id).order('scheduled_start',{ascending:true});s.appointments=ap.data||[];","let appointmentQuery=c.from('organization_appointments').select('*').order('scheduled_start',{ascending:true});if(s.role!=='admin')appointmentQuery=appointmentQuery.eq('organization_id',s.org.id);const ap=await appointmentQuery;s.appointments=ap.data||[];"],
  ["<div class=\"kcp-org\"><span>Organisation</span><strong>${esc(s.org?.name||'—')}</strong><small>${esc(s.org?.clinical_data_status||'—')}</small></div>","<div class=\"kcp-org\"><span>${s.role==='admin'?'Vue globale':'Centre actif'}</span>${s.role==='admin'?`<strong>Tous les centres</strong>`:s.memberships.length>1?`<select data-kcp-org-select aria-label=\"Choisir le centre actif\">${s.memberships.map(m=>`<option value=\"${m.organization_id}\" ${m.organization_id===s.org?.id?'selected':''}>${esc(m.organizations?.name||'Centre KŌMØ')}</option>`).join('')}</select>`:`<strong>${esc(s.org?.name||'—')}</strong>`}<small>${s.role==='admin'?'Admin KŌMØ · accès global':`${esc(s.membership?.role||'—')} · ${esc(s.membership?.access_scope||'—')} · ${esc(s.org?.clinical_data_status||'—')}`}</small></div>"],
  ["<div class=\"kcp-kpi\"><span>Patients</span><strong>${s.patients.length}</strong><small>organisation</small></div>","<div class=\"kcp-kpi\"><span>Patients</span><strong>${s.patients.length+(s.role==='admin'?s.pulseAccounts.length:0)}</strong><small>${s.role==='admin'?'tous comptes':'organisation'}</small></div>"],
  ["<h3>Patients</h3><p>Dossier transversal relié à Motion, Clinical, plans et rendez-vous.</p>","<h3>${s.role==='admin'?'Tous les patients':['owner','clinical_admin'].includes(s.membership?.role)?'Patients du centre':'Mes patients'}</h3><p>${s.role==='admin'?'Dossiers de tous les centres et comptes Pulse encore sans dossier.':['owner','clinical_admin'].includes(s.membership?.role)?'Tous les dossiers autorisés dans ce centre.':'Uniquement les patients qui vous sont explicitement affectés.'}</p>"],
  ["<div class=\"kcp-table-wrap\"><table class=\"kcp-patient-table\"><thead><tr><th>Patient</th><th>Dernier bilan</th><th>Motion</th><th>Prochain RDV</th><th>État</th></tr></thead><tbody id=\"kcpPatientsBody\">${patientRows(s.patients)}</tbody></table></div></article></div>`}","<div class=\"kcp-table-wrap\"><table class=\"kcp-patient-table\"><thead><tr><th>Patient</th><th>Dernier bilan</th><th>Motion</th><th>Prochain RDV</th><th>État</th></tr></thead><tbody id=\"kcpPatientsBody\">${patientRows(s.patients)}</tbody></table></div>${s.role==='admin'&&s.pulseAccounts.length?`<div class=\"kcp-card-head\" style=\"margin-top:22px\"><div><h3>Comptes Pulse sans dossier</h3><p>Inscrits dans Pulse, pas encore orientés vers un centre.</p></div><strong>${s.pulseAccounts.length}</strong></div><div class=\"kcp-list\">${s.pulseAccounts.map(x=>{const p=x.profile||{},n=`${p.first_name||''} ${p.last_name||''}`.trim()||p.display_name||x.email||'Patient Pulse';return`<div class=\"kcp-row\"><div class=\"kcp-row-main\"><strong>${esc(n)}</strong><span>${esc(x.email||'—')} · ${esc([p.city,p.country].filter(Boolean).join(', ')||'Localisation non renseignée')}</span></div><span>Pulse uniquement</span>${statePill('à orienter')}<button class=\"kcp-btn\" data-open-global-admin=\"1\">Orienter</button></div>`}).join('')}</div>`:''}</article></div>`}"],
  ["document.querySelectorAll('[data-switch]').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.switch)));","document.querySelectorAll('[data-switch]').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.switch)));document.querySelectorAll('[data-open-global-admin]').forEach(b=>b.addEventListener('click',()=>{location.hash='admin';setTimeout(()=>window.dispatchEvent(new CustomEvent('komo:admin-open')),40)}));"]
];
for(const [from,to] of replacements){if(!src.includes(from))throw new Error('[clinical-multicenter-v1] expected source fragment missing: '+from.slice(0,90));src=src.replace(from,to)}
await writeFile(cockpitPath,src);

let html=await readFile(indexPath,'utf8');
if(!html.includes('./center-context-v1.css')) html=html.replace('<link rel="stylesheet" href="./admin-console-v2.css" />','<link rel="stylesheet" href="./admin-console-v2.css" />\n  <link rel="stylesheet" href="./center-context-v1.css" />');
if(!html.includes('./center-context-v1.js')) html=html.replace('<script type="module" src="./admin-console-v2.js"></script>','<script type="module" src="./admin-console-v2.js"></script>\n  <script src="./center-context-v1.js"></script>');
await writeFile(indexPath,html);
console.log('[clinical-multicenter-v1] multi-center organization, global Admin patient visibility and assets applied');
