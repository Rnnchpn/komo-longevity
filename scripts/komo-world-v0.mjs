import { cp, mkdir, rm, readFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = dirname(dirname(fileURLToPath(import.meta.url)));
const source = join(root, 'src', 'world');
const target = join(root, 'site', 'world');

const html=await readFile(join(source,'index.html'),'utf8');
const runtime=await readFile(join(source,'world-v1.js'),'utf8');
const css=await readFile(join(source,'world-v1.css'),'utf8');
const multiplayer=await readFile(join(source,'world-multiplayer-v1.js'),'utf8');
const pulseAuth=await readFile('pulse-app/auth-gateway-v2.js','utf8');
const pulseHtml=await readFile('pulse-app/index.html','utf8');
const checks=[
  ['V7.1 desktop HTML cache bust present',html.includes('world-v1.css?v=7.1.0-oversized-mascot')&&html.includes('world-v1.js?v=7.1.0-oversized-mascot')&&html.includes('name="komo-world-version" content="7.1.0-oversized-mascot"')],

  ['canonical World V1 runtime',runtime.includes("window.KomoWorld={")],
  ['render loop present',runtime.includes('renderer.render(scene,camera)')],
  ['living animation owner present',runtime.includes('function animateLiving(now)')],
  ['living animation invoked safely',runtime.includes('try{animateLiving(now)}')],
  ['retired undefined updateLiving call absent',!runtime.includes('updateLiving(now)')],
  ['multiplayer remains optional',runtime.includes("import('./world-multiplayer-v1.js?v=20260924-avatar-v140')")&&runtime.includes('.catch(err=>console.warn')],
  ['true sky V6.3.2 present',runtime.includes('KOMO_TRUE_SKY_V18')&&runtime.includes('KOMO_CLOUD_FIELD_V632')&&runtime.includes('uniform vec3 zenithColor')&&runtime.includes('uniform vec3 hazeColor')],
  ['second floor V1.8 present',runtime.includes('KOMO_UPPER_LEVEL_V18')&&runtime.includes('KOMO_GRAND_STAIR_V18')],
  ['upper-floor navigation present',runtime.includes('syncPlayerElevation')&&runtime.includes('isUpperWalkable')],
  ['premium flooring V1.9 present',runtime.includes('KOMO_FLOORING_V19')&&runtime.includes('KOMO_EXTERIOR_FLOOR_V19')],
  ['ambient people V1.9 present',runtime.includes('KOMO_AMBIENT_PEOPLE_V19')&&runtime.includes('function updateNpc')],
  ['avatar presence V2.0+ present',runtime.includes('function npcNameTag')&&runtime.includes('lastFarUpdate')],
  ['avatar LOD V2.0 present',runtime.includes('lastFarUpdate')&&runtime.includes('distToCamera')],
  ['floor polish V2.0 present',runtime.includes('V2.0 floor polish')],
  ['smooth UX V2.1+ present',runtime.includes('function fastTravel')&&runtime.includes('world-menu')],
  ['adaptive performance V2.1+ present',runtime.includes('function updatePerformance')&&runtime.includes('applyRenderScale')],
  ['performance rescue V2.2+ present',runtime.includes('function applyEmergencyPerformance')&&runtime.includes('function freezeStaticScene')],
  ['low-power lights disabled',runtime.includes('l.visible=false;l.intensity=0')&&runtime.includes("antialias:!lowPower")],
  ['living entrance V2.3+ present',runtime.includes('KOMO_LIVING_ENTRANCE_V23')&&runtime.includes('function updateDoors(now,dt)')],
  ['arrival life cues V2.3 present',runtime.includes('KOMO_ARRIVAL_DETAILS_V23')],
  ['third-person V7 present',runtime.includes('KOMO_PLAYER_AVATAR_V71_OVERSIZED')&&runtime.includes("cameraMode='third'")&&runtime.includes('const eyeY=player.y+visualGround+2.03+bob')],
  ['World Journey V2.4+ present',runtime.includes('KOMO_WORLD_JOURNEY_V24')&&runtime.includes('const JOURNEY_MISSIONS')&&runtime.includes('function completeJourney')],
  ['auto-run gameplay V5.3 present',runtime.includes('const AUTO_RUN_SPEED=lowPower?6.05:7.05')&&runtime.includes('const AUTO_RUN_BOOST=lowPower?7.15:8.35')&&runtime.includes('tryMoveSmooth')],
  ['living journey V2.5+ present',runtime.includes('KOMO_JOURNEY_GUIDE_V25')&&runtime.includes('function updateJourneyGuide')],
  ['NPC conversations V2.5 present',runtime.includes('function showNpcConversation')&&runtime.includes("id:'social'")],
  ['smart third-person camera V2.5 present',runtime.includes('thirdPersonDistance')&&runtime.includes('Cheap camera collision clamp')],
  ['Twin Lab V2.6+ present',runtime.includes('KOMO_TWIN_LAB_V26')&&runtime.includes('const twinInteractions')],
  ['Rehab Lab V2.6+ present',runtime.includes('KOMO_REHAB_LAB_V26')&&runtime.includes('const rehabInteractions')&&runtime.includes('function runRehabDemo')],
  ['One World interaction pool present',runtime.includes("const pool=[...interactions,...twinInteractions,...rehabInteractions,...arenaInteractions]")&&runtime.includes('function updateTwinVisuals')],
  ['Biomechanical Twin V2.7+ present',runtime.includes('KOMO_BIOMECH_TWIN_V27')&&runtime.includes('function updateBiomechTwin')],
  ['Rehab Coach V2.7 present',runtime.includes('KOMO_REHAB_COACH_V27')&&runtime.includes('function animateRehabCoach')&&runtime.includes("id:'rehab_coach'")],
  ['FPS budget V2.8+ present',runtime.includes('function updateLightBudget')&&runtime.includes('function updateVisibilityBudget')],
  ['shadow budget V2.8 present',runtime.includes('renderer.shadowMap.enabled=false')&&runtime.includes("qualityMode==='high'")],
  ['draw-call telemetry V2.8 present',runtime.includes('renderer.info.render.calls')&&runtime.includes('activeLightBudget')],
  ['Fitness Club V2.9+ present',runtime.includes('KOMO_FITNESS_CLUB_V29')&&runtime.includes('const FITNESS_ACTIVITIES')],
  ['daily Fitness program V2.9 present',runtime.includes('function fitnessToday')&&runtime.includes('function fitnessStreak')&&runtime.includes('function markFitnessTodayComplete')],
  ['Fitness Coach V2.9 present',runtime.includes('function runFitnessCoachPreview')&&runtime.includes('ALEX · FITNESS COACH')],
  ['Hall Living V3+ present',runtime.includes('KOMO_HALL_LIVING_V30')&&runtime.includes('function instancedStatic')],
  ['Hall Living social layer present',runtime.includes("label:'Camille'")&&runtime.includes("label:'Lina'")&&runtime.includes('TODAY AT KŌMØ')],
  ['Hall Living culling present',runtime.includes('hallLiving.visible=player.z<19&&player.z>-29')&&runtime.includes('mesh:m')],
  ['Retina sharpness V3.0.2+ present',runtime.includes('const retinaMobile=lowPower&&deviceDpr>=2')&&runtime.includes('pixelRatio:renderer.getPixelRatio()')],
  ['mobile no forced rescue V3.0.2+ present',!runtime.includes('if(lowPower)applyEmergencyPerformance();')&&runtime.includes('retinaMobile?1.02:.86')&&runtime.includes('retinaMobile?.78:.68')],
  ['Visual polish V7 present',runtime.includes('KOMO_PLAYER_AVATAR_V71_OVERSIZED')&&runtime.includes('KOMO_HALL_HOST_V31')&&runtime.includes('THREE.CapsuleGeometry')],
  ['V7 player grounding present',runtime.includes("bodyRoot.name='KOMO_PLAYER_BODY_GROUNDING_V70'")&&runtime.includes('leftElbow')&&runtime.includes('rightElbow')&&runtime.includes('av.tag.visible=false')],
  ['World Hub V3.2+ present',runtime.includes('KOMO_WORLD_DISTRICT_V32')&&runtime.includes('KOMO_GRAND_FOUNTAIN_V32')],
  ['World Hub destination doors present',runtime.includes('KOMO_DESTINATION_DOOR_')&&runtime.includes('function updateDestinationDoors')],
  ['World Hub health + challenges present',runtime.includes('function showHealthOverview')&&runtime.includes('const CHALLENGE_KEY')&&runtime.includes('KOMO_CHALLENGE_BOARD_V32')],
  ['World Hub Life items present',runtime.includes('KOMO_LIFE_ITEMS_V32')&&runtime.includes('function showLifeItem')],
  ['World Hub avatar studio present',runtime.includes('AVATAR_KEY')&&runtime.includes('function showAvatarStudio')],
  ['V6.3.2 adaptive clouds present',runtime.includes('const cloudCount=lowPower?1:9')&&runtime.includes('const cloudTextures=[makeCloudTexture(1),makeCloudTexture(4),makeCloudTexture(7)]')&&runtime.includes('cloud.userData.baseOpacity')&&runtime.includes('if(living.clouds?.length)')],
  ['Immersive Hub V6.4 entry present',runtime.includes("version:'7.1.0-oversized-mascot'")&&runtime.includes('KOMO_HEALTH_STATION_V33')&&runtime.includes('KOMO_ENTRY_GUIDE_V33')],
  ['Immersive Hub V4.5 portals present',runtime.includes('KOMO_PORTAL_ARCH_V45_')&&runtime.includes('KOMO_DESTINATION_VESTIBULES_V45')&&runtime.includes('function updateDestinationDoors')],
  ['Immersive Hub V3.3 Life retail present',runtime.includes('KOMO_LIFE_RETAIL_WALL_V33')&&runtime.includes("id:'life_jacket'")&&runtime.includes("id:'life_band'")],
  ['Immersive Hub NPC roles present',runtime.includes("quest:'leo'")&&runtime.includes("quest:'theo'")&&runtime.includes("quest:'maya'")&&runtime.includes("functionLabel:'FITNESS COACH'")&&runtime.includes("functionLabel:'ARENA COACH'")],
  ['Immersive Hub V3.3 district detail present',runtime.includes('KOMO_DISTRICT_DETAILS_V33')&&runtime.includes('HEALTH PAVILION')&&runtime.includes('CLUB HOUSE')],
  ['multiplayer auth isolation V1.3 present',multiplayer.includes("version:'1.4.0-oversized-avatar'")&&multiplayer.includes("persistSession:false")&&multiplayer.includes("autoRefreshToken:false")&&!multiplayer.includes("storageKey:'komo-world-auth-v1'")],
  ['push-to-talk voice control present',multiplayer.includes('MAINTENIR POUR PARLER')&&multiplayer.includes('const startTalking=async')&&multiplayer.includes("U.talk.addEventListener('pointerdown'")&&multiplayer.includes('track.enabled=state.voice.talking')],
  ['voice signaling fallback present',multiplayer.includes('const pollVoiceSignals=async')&&multiplayer.includes("setInterval(()=>pollVoiceSignals(),350)")&&multiplayer.includes("VOICE_EXIT_M=22")],
  ['desktop social HUD present',multiplayer.includes('kwmp-chat-launcher')&&multiplayer.includes('kwmp-voicebox')&&multiplayer.includes("drawer.className='kwmp-chat'")&&multiplayer.includes('const openChat=()=>')],
  ['desktop clarity nav present',html.includes('data-nav-zone="hall"')&&html.includes('data-nav-zone="twin"')&&html.includes('data-nav-zone="life"')&&html.includes('data-nav-zone="upper"')&&runtime.includes('function syncQuickNav(zone)')],
  ['My World information menu present',html.includes('id="results-toggle"')&&html.includes('id="campus-toggle"')&&html.includes('id="journey-toggle"')&&html.includes('world-menu-summary')&&runtime.includes('function showCampusMap')],
  ['V6.1 command center menu present',html.includes('world-command-center')&&html.includes('id="menu-current-zone"')&&html.includes('world-destinations')&&css.includes('V6.1 Grand Flagship UI')&&runtime.includes('menuCurrentZone.textContent=label')],
  ['V6.1 premium HUD hierarchy present',css.includes('.journey-hud:before,.health-hud:before')&&css.includes('.world-panel:before')&&css.includes('.world-panel .panel-actions button.primary')],
  ['V7 optional avatar entry present',html.includes('id="intro-avatar-open"')&&!html.includes('id="intro-avatar-config"')&&runtime.includes("if(introAvatarOpen)introAvatarOpen.addEventListener")&&runtime.includes("performance:{roughness:.82")&&css.includes('V6.2 Intro — avatar customisation is optional')],
  ['V6.1 avatar clothing variants present',runtime.includes("look:'tailored'")&&runtime.includes("avatarConfig.look==='performance'")&&runtime.includes("avatarConfig.look==='riviera'")&&runtime.includes('garments:{torso,chest,waist')],

  ['V7.1 oversized avatar rig present',runtime.includes("g.name='KOMO_PLAYER_AVATAR_V71_OVERSIZED'")&&runtime.includes('const makeShoe=')&&runtime.includes('const makeHand=')&&runtime.includes('const shoulderGeo=')&&multiplayer.includes("g.name='KOMO_REMOTE_V71_'")&&multiplayer.includes('const makeShoe=')&&multiplayer.includes('const makeHand=')],
  ['V7.1 minimal face present',runtime.includes('// V7.1 minimal face')&&!runtime.includes('const eye=mesh(headGroup')&&!runtime.includes('const mouth=mesh(headGroup')&&multiplayer.includes('new THREE.SphereGeometry(.084,12,9)')],
  ['V7.1 oversized sleeves present',runtime.includes('capsule(.064,.300,cloth,leftArm')&&runtime.includes('const leftSleeve=capsule(.057,.285,clothDark')&&multiplayer.includes('capsule(.064,.300,matOut,leftArm')],
  ['V7.1 KOMO mascot present',runtime.includes("g.name='KOMO_MASCOT_V71'")&&runtime.includes('function updateKomoMascot')&&runtime.includes("x.fillText('K',128,126)")&&runtime.includes('const komoMascot=makeKomoMascotV71()')],
  ['V7 avatar bilateral rig present',runtime.includes('const leftShoe=makeShoe(leftKnee),rightShoe=makeShoe(rightKnee)')&&runtime.includes('const leftHand=makeHand(leftElbow),rightHand=makeHand(rightElbow)')&&multiplayer.includes('const leftShoe=makeShoe(leftKnee),rightShoe=makeShoe(rightKnee)')&&multiplayer.includes('const leftHand=makeHand(leftElbow),rightHand=makeHand(rightElbow)')],
  ['V7 human gait present',runtime.includes('const runBlend=THREE.MathUtils.smoothstep(moveAmount,.68,.98)')&&runtime.includes('const strideAmp=THREE.MathUtils.lerp(.34,.58,runBlend)*moveAmount')&&runtime.includes('av.torsoGroup.rotation.x=-THREE.MathUtils.lerp(.004,.038,runBlend)*moveAmount')&&multiplayer.includes('const legAmp=THREE.MathUtils.lerp(.28,.50,runBlend)')],
  ['V7 matte human materials present',runtime.includes('roughness:.86,metalness:0')&&runtime.includes('roughness:.78,metalness:.004')&&runtime.includes('color:0xe4e0d7')&&runtime.includes('color:0xa98658,roughness:.48,metalness:.24')],
  ['V7 premium idle present',runtime.includes('const idleBreath=Math.sin(now*.00115)')&&runtime.includes('const idleWeight=Math.sin(now*.00052)')&&runtime.includes('av.headGroup.rotation.y=Math.sin(now*.00034)*.016')],
  ['V7.1 oversized proportions present',runtime.includes('hips.scale.set(1.18,.58,.82)')&&runtime.includes('new THREE.CylinderGeometry(.285,.245,.625')&&runtime.includes('new THREE.CapsuleGeometry(.034,.064')&&runtime.includes('const jacketYoke=box')&&multiplayer.includes('q.scale.set(1.18,.58,.82)')&&multiplayer.includes('new THREE.CylinderGeometry(.285,.245,.625')],
  ['One World continuous campus present',runtime.includes("version:'7.1.0-oversized-mascot'")&&runtime.includes('KOMO_ONE_WORLD_LINKS_V37')&&runtime.includes('function inTwinZone')&&runtime.includes("mode='world';\n  world.visible=true;twinRoom.visible=true;rehabRoom.visible=true;arenaRoom.visible=true")],
  ['transient zone label present',runtime.includes("function showWorldZone(label,purpose='')")&&runtime.includes("locationChip?.classList.add('show')")&&runtime.includes("locationPurpose.textContent=purpose")],
  ['results dashboard V4.1 present',runtime.includes('results-hero-v41')&&runtime.includes('score-orbit')&&runtime.includes('results-domains')&&runtime.includes('results-timeline')&&runtime.includes('results-signals')],
  ['hall lighting V6.3.1 compatible',runtime.includes('KOMO_HALL_LIGHTING_V43')&&runtime.includes('KOMO_HALL_PRACTICALS_V43')&&runtime.includes('function addHallSpot')&&runtime.includes('hallLightProfile')&&runtime.includes('renderer.toneMappingExposure=lowPower?1.17:1.22')],
  ['hall spatial accents V4.3 present',runtime.includes("color:0xdceae0,intensity:17")&&runtime.includes("color:0xffdda2,intensity:19")&&runtime.includes("color:0xe8c98f,intensity:17")&&runtime.includes('Destination thresholds are brighter in geometry')],
  ['hall lighting performance budget V4.3 present',runtime.includes("hallLightGroup.visible=!lowPower&&!emergencyPerformance&&player.z<22&&player.z>-31")&&runtime.includes("qualityMode==='performance'?.16:.34")],
  ['hall materials V4.4 present',runtime.includes('V4.4 Materials Realism')&&runtime.includes("kind==='travertine'")&&runtime.includes("kind==='plaster'")&&runtime.includes("kind==='walnut'")&&runtime.includes("kind==='fabric'")&&runtime.includes("kind==='leather'")&&runtime.includes('transmission:.74')&&runtime.includes('KOMO_HALL_MATERIAL_FINISH_V44')],
  ['material bump performance V4.4 present',runtime.includes('bumpScale:lowPower?0')&&runtime.includes('const TEX_SIZE=lowPower?256:512')&&runtime.includes('TEX_ANISO')],
  ['PBR normal pipeline V6 present',runtime.includes('function normalTextureFromHeight')&&runtime.includes("kind==='micro'")&&runtime.includes('const S_SAGE=')&&runtime.includes('const S_BLACKENED=')],
  ['PBR low-power safety V6 present',runtime.includes('if(lowPower)return null')&&runtime.includes('normalMap?{normalMap}:{}')&&runtime.includes('bumpScale:lowPower?0')],
  ['floor normals V6 present',runtime.includes('normalTextureFromHeight(raw.height,repeat,1.55)')&&runtime.includes('normalScale:new THREE.Vector2(.52,.52)')],

  ['physical glass V4.4 present',runtime.includes('ior:1.48')&&runtime.includes('clearcoat:.34')&&runtime.includes('transmission:.74')],
  ['leather case V4.4 present',runtime.includes('const shell=MAT.leatherDark')&&runtime.includes('const leather=MAT.leather')],
  ['wall articulation V4.4 present',runtime.includes('wallWarm=M.wall.clone()')&&runtime.includes('10 mm shadow-reveal between panels')&&runtime.includes('floating bronze datum line')],
  ['architectural wall palette V5.9 present',runtime.includes('V5.9 wall architecture palette')&&runtime.includes('const WALL={')&&runtime.includes('WALL.ivory.color.setHex')],
  ['hall architectural walls V5.9 present',runtime.includes('KOMO_HALL_WALLS_V59')&&runtime.includes('Hero niches punctuate the long nave')&&runtime.includes('hallWallGlow')],
  ['Twin architectural walls V5.9 present',runtime.includes('KOMO_TWIN_WALLS_V59')&&runtime.includes('Twin Data Wall')&&runtime.includes('twinWallGlow')],
  ['Fitness architectural walls V5.9 present',runtime.includes('KOMO_FITNESS_WALLS_V59')&&runtime.includes('warm acoustic timber')&&runtime.includes('fitnessWallMirror')],
  ['Arena architectural walls V5.9 present',runtime.includes('KOMO_ARENA_WALLS_V59')&&runtime.includes('blackened ribs')&&runtime.includes('arenaWallGlow')],
  ['wall lighting performance V5.9 present',runtime.includes('no extra dynamic lights are used')&&runtime.includes("typeof hallWallGlow!=='undefined'")],
  ['baked wall wash V6 present',runtime.includes('KOMO_REALISM_LIGHT_WASHES_V60')&&runtime.includes('function makeWallWashTexture')&&runtime.includes('KOMO_HALL_WASH_INST_V60')&&runtime.includes('KOMO_ARENA_WASH_INST_V60')],
  ['V6.3.1 lighting role separation present',runtime.includes('KOMO_LIGHTING_STRUCTURAL_V631')&&runtime.includes('KOMO_LIGHTING_DECORATIVE_V631')&&runtime.includes('KOMO_LIGHTING_DESTINATION_V631')],
  ['V6.3.1 structural skylight present',runtime.includes('KOMO_STRUCTURAL_SKYLIGHT_INST_V631')&&runtime.includes('KOMO_STRUCTURAL_BOUNCE_INST_V631')&&runtime.includes("makeLightingGradientV631('ceiling')")],
  ['V6.3.1 room lighting identities present',runtime.includes('KOMO_LIGHTING_DECORATIVE_TWIN_V631')&&runtime.includes('KOMO_LIGHTING_DECORATIVE_FITNESS_V631')&&runtime.includes('KOMO_LIGHTING_DECORATIVE_ARENA_V631')&&runtime.includes('0xc5dbcf')&&runtime.includes('0xe8c997')&&runtime.includes('0xc9975d')],
  ['V6.3.1 destination guidance present',runtime.includes('destinationCoolMatV631')&&runtime.includes('destinationFitMatV631')&&runtime.includes('destinationArenaMatV631')&&runtime.includes('new THREE.PlaneGeometry(4.7,9.5)')],
  ['V6.3.1 shared realtime light budget present',runtime.includes("new Set([...living.lights,...hallLights])")&&runtime.includes('profileIntensity')&&runtime.includes("l.userData.lightingRole='decorative'")],
  ['V6.3.1 lighting LOD and rescue present',runtime.includes('L.structural.visible=hallDetail')&&runtime.includes('decorativeTwinV631.visible=twinD')&&runtime.includes('L.destination.visible=player.z<8')&&runtime.includes('living.lightingV631.root.visible=false')],
  ['V6.3.1 daylight lighting profiles present',runtime.includes('function applyLightingV631Profile')&&runtime.includes("golden:{structural:.80,decorative:1.12,destination:1.08}")&&runtime.includes('applyLightingV631Profile(state)')],
  ['V6.3.2 natural sky gradient present',runtime.includes('Four-stage natural gradient')&&runtime.includes('float horizonBand=exp')&&runtime.includes('uniform float horizonSoftness')&&!runtime.includes('sunCore=pow')],
  ['V6.3.2 subtle solar halo present',runtime.includes('Broad atmospheric solar glow only')&&runtime.includes('float sunGlow=pow(s,18.0)')&&runtime.includes('float sunHalo=pow(s,5.5)')],
  ['V6.3.2 horizon haze present',runtime.includes('KOMO_ATMOSPHERE_V632')&&runtime.includes('horizonHazeUniformsV632')&&runtime.includes('new THREE.CylinderGeometry(154,166,54,64,1,true)')],
  ['V6.3.2 grounded landscape skirt present',runtime.includes('new THREE.PlaneGeometry(310,310)')&&runtime.includes('distantGroundMatV632')&&runtime.includes('hides the edge of the playable ground')],
  ['V6.3.2 Riviera horizon instancing present',runtime.includes('KOMO_RIVIERA_HILLS_NEAR_INST_V632')&&runtime.includes('KOMO_RIVIERA_HILLS_FAR_INST_V632')&&runtime.includes('KOMO_RIVIERA_PINES_INST_V632')&&runtime.includes('KOMO_RIVIERA_PAVILIONS_INST_V632')],
  ['V6.3.2 daylight atmosphere profiles present',runtime.includes('function applyAtmosphereV632Profile')&&runtime.includes('morning:{haze:')&&runtime.includes('golden:{haze:')&&runtime.includes('evening:{haze:')&&runtime.includes('applyAtmosphereV632Profile(state)')],
  ['V6.3.2 fog depth present',runtime.includes('fogNear=lowPower?96:72')&&runtime.includes('fogFar=lowPower?238:188')&&runtime.includes('scene.fog.near=fogNear;scene.fog.far=fogFar')],
  ['V6.3.2 atmosphere performance budget present',runtime.includes('const pineCount=lowPower?8:24')&&runtime.includes('const pavilionCount=lowPower?4:8')&&runtime.includes('living.atmosphereV632.visible=!emergencyPerformance')&&runtime.includes('living.atmosphereV632.visible=false')],
  ['V6.3.2 visible arrival hero present',runtime.includes('KOMO_ARRIVAL_HERO_V632')&&runtime.includes('KOMO_ARRIVAL_PIERS_INST_V632')&&runtime.includes('KOMO_ARRIVAL_BOUNDARY_INST_V632')&&runtime.includes("'KŌMØ WORLD','LONGEVITY IN MOTION'")],
  ['V6.3.2 arrival canopy present',runtime.includes('arrivalRoofMatV632')&&runtime.includes('19.6,.22,7.4')&&runtime.includes('arrivalReflectionMatV632')],
  ['V6.9 arrival parity budget present',runtime.includes("arrivalHeroV632.visible=!emergencyPerformance")&&runtime.includes("arrivalHeroV632.visible=false")],
  ['V6.4 living campus root present',runtime.includes('KOMO_LIVING_CAMPUS_V64')&&runtime.includes('living.livingCampusV64=livingCampusV64')],
  ['V6.4 hero trees present',runtime.includes('function heroTreeV64')&&runtime.includes('KOMO_HERO_TREE_V64')&&runtime.includes('heroTreeSpecsV64')],
  ['V7 premium avatar framing present',runtime.includes("let thirdPersonDistance=5.35")&&runtime.includes("g.name='KOMO_PLAYER_AVATAR_V71_OVERSIZED'")&&runtime.includes("const desiredFov=50+2.4*runAmount")],
  ['V6.5 arrival visual presence present',runtime.includes("'MOTION HOUSE','MEASURE · MOVE'")&&runtime.includes("'LONGEVITY LAB','SCIENCE · LIVE'")&&runtime.includes("waterMirrorV64(-10.6,13.5,2.4,9.6)")],
  ['V6.9 shared long-range LOD present',runtime.includes("npc.visible=allowed&&sameLevel&&dist<68")&&runtime.includes("banner.visible=wp.distanceTo(camera.position)<96")],
  ['V6.8 compact Riviera masterplan present',runtime.includes('KOMO_RIVIERA_MASTERPLAN_V68')&&runtime.includes('KOMO_MARINA_V68')&&runtime.includes('KOMO_RETREAT_VILLA_V68')],
  ['V6.8 marina reads as yachting destination',runtime.includes("'BOARDING LOUNGE','KŌMØ YACHTING · RETREATS'")&&runtime.includes("yachtV68(marinaV68,81.0,65.5,27")&&runtime.includes("'KŌMØ MARINA','RIVIERA · YACHTING'")],
  ['V6.8 retreat villa program present',runtime.includes("'KŌMØ RETREAT VILLA','PRIVATE LONGEVITY EXPERIENCE'")&&runtime.includes("'LONGEVITY TABLE','NUTRITION · CONVERSATION'")&&runtime.includes("'SKIN + NAD','LONGEVITY'")],
  ['V6.8 territories are walkable',runtime.includes('function inMarinaZone')&&runtime.includes('function inVillaZone')&&runtime.includes('inMarinaLink(p)||inVillaLink(p)')],
  ['V6.8 navigation exposes territories',runtime.includes("marina:{mode:'world'")&&runtime.includes("villa:{mode:'world'")&&html.includes('data-fast-travel="marina"')&&html.includes('data-fast-travel="villa"')],
  ['V6.8 campus map exposes ecosystem',runtime.includes('class="campus-node marina"')&&runtime.includes('class="campus-node villa"')&&css.includes('.campus-node.marina')&&css.includes('.campus-node.villa')],
  ['V6.8 compact proportions present',runtime.includes('KOMO_RIVIERA_LANDSCAPE_V68')&&runtime.includes("gatewayPergolaV68(wayfindingV68,35.2,67.8,'MARINA'")&&runtime.includes('box(marinaV68,23,.18,58')&&runtime.includes('retreatHouseV68.position.set(-54,0,79)')],
  ['V6.8 architectural landmarks present',runtime.includes("'YACHTING HOUSE','PRIVATE EXPERIENCE · HOSPITALITY'")&&runtime.includes("'PRIVATE SUITES','SLEEP · RECOVERY'")&&runtime.includes('const upperSuiteV68=new THREE.Group()')],
  ['V6.8 territorial life rebalanced',runtime.includes("label:'Alba'")&&runtime.includes("x:47.0,y:0,z:55.0")&&runtime.includes("label:'Clara'")&&runtime.includes("x:-43.0,y:0,z:62.0")],
  ['V6.9 same camera framing across devices',runtime.includes('new THREE.PerspectiveCamera(54')&&runtime.includes('const desiredFov=50+2.4*runAmount')&&runtime.includes('let thirdPersonDistance=5.35')],
  ['V6.9 same campus geometry across devices',!runtime.includes('const heroTreeSpecsV64=lowPower?')&&runtime.includes('const shrubCountV64=44')&&runtime.includes("campusPavilionV64(-17.8,14.0,7.8,8.8,'MOTION HOUSE'")],
  ['V6.9 same estate composition across devices',runtime.includes("yachtV68(marinaV68,79.5,81.2,21,Math.PI/2,'RIVIERA')")&&!runtime.includes("if(!lowPower)yachtV68(marinaV68,79.5,81.2,21")&&runtime.includes('].forEach(v=>oliveTreeV68(villaDetailV68,...v))')],
  ['V6.9 structural visibility parity',runtime.includes('M.marinaDetail.visible=!emergencyPerformance;')&&runtime.includes('M.villaDetail.visible=!emergencyPerformance;')&&runtime.includes('tree.visible=wp.distanceTo(camera.position)<(hero?130:96)')],
  ['V6.4 landscape instancing present',runtime.includes('KOMO_CYPRESS_CROWNS_INST_V64')&&runtime.includes('KOMO_PINE_CROWNS_INST_V64')&&runtime.includes('KOMO_LANDSCAPE_SHRUBS_INST_V64')],
  ['V6.4 premium pavilions present',runtime.includes("'MOVEMENT','MEASURE · TRAIN · MOVE'")&&runtime.includes("'LONGEVITY','SCIENCE · TRAJECTORY'")&&runtime.includes("'COMMUNITY LOUNGE','MEET · CONNECT · RECOVER'")&&runtime.includes("'RECOVERY HOUSE','REST · RESET · RESTORE'")],
  ['V6.4 water mirrors present',runtime.includes('function waterMirrorV64')&&runtime.includes('waterMirrorV64(-24.6,45.0,5.4,24.0)')&&runtime.includes('waterMirrorV64(0,89.2,18.0,7.2)')],
  ['V6.4 rear terrace waterfall present',runtime.includes('KOMO_REAR_TERRACE_V64')&&runtime.includes('function waterfallTextureV64')&&runtime.includes('living.waterfallsV64.push')],
  ['V6.4 ambient crowd instancing present',runtime.includes('KOMO_AMBIENT_CROWD_INST_V64')&&runtime.includes('KOMO_CAMPUS_PEOPLE_BODY_INST_V64')&&runtime.includes('KOMO_CAMPUS_PEOPLE_HEAD_INST_V64')],
  ['V6.4 moving campus NPCs present',runtime.includes("label:'Iris'")&&runtime.includes("label:'Louis'")&&runtime.includes("label:'Eva'")&&runtime.indexOf("label:'Iris'")>runtime.indexOf("const npcRoot=new THREE.Group()")],
  ['V6.4 living animation present',runtime.includes('living.waterfallsV64?.length')&&runtime.includes('w.texture.offset.y=-(t*.045')],
  ['V6.9 campus parity performance budget present',runtime.includes("living.livingCampusV64.visible=!emergencyPerformance")&&runtime.includes("campusCrowdV64.visible=!emergencyPerformance")&&runtime.includes("living.livingCampusV64.visible=false")],
  ['V6.9 hero tree LOD parity present',runtime.includes("tree.name==='KOMO_HERO_TREE_V64'")&&runtime.includes("(hero?130:96)")],




  ['realism LOD V6 present',runtime.includes('function updateRealismLOD')&&runtime.includes('realismLightWashesV60.visible=!emergencyPerformance')&&runtime.includes('galleryWallsV59.visible=nearGallery')],
  ['gallery instancing V6 present',runtime.includes('const galleryBatches=')&&runtime.includes('KOMO_GALLERY_PIER_INST_V60')&&runtime.includes('KOMO_GALLERY_WALNUT_INST_V60')],

  ['walkable gallery walls V6 instanced',runtime.includes('KOMO_GALLERY_WALLS_V59')&&runtime.includes('function galleryBaySpec')&&runtime.includes('KOMO_GALLERY_BASE_INST_V60')&&runtime.includes('KOMO_GALLERY_GLASS_INST_V60')],
  ['architectural spine V4.5 present',runtime.includes('KOMO_ARCHITECTURAL_SPINE_V45')&&runtime.includes('Five repeated portal frames establish scale')&&runtime.includes('Continuous dark reveal around the central skylight')],
  ['architectural bays V4.5 present',runtime.includes('KOMO_RECESSED_BAY_V45_')&&runtime.includes('Deep black reveal gives the bay a built-in appearance')&&runtime.includes('KOMO_CEILING_RAFT_V45')],
  ['destination vestibules V4.5 present',runtime.includes('KOMO_DESTINATION_VESTIBULES_V45')&&runtime.includes('1.8 m deep vestibule')&&runtime.includes('KOMO_PORTAL_ARCH_V45_')],
  ['architectural wayfinding V4.5 present',runtime.includes('KOMO_DESTINATION_DATUM_V45')&&runtime.includes('KOMO_ATRIUM_FOCAL_V45')&&runtime.includes('align decorative rhythm with V4.5')===false],
  ['desktop cinematic V5 present',runtime.includes('KOMO_DESKTOP_CINEMATIC_V50')&&runtime.includes('Polished central runway')&&runtime.includes('Soft volumetric shafts')&&runtime.includes('One quiet monolithic focal wall')],
  ['KŌMØ WORLD floor mosaic V5.1 present',runtime.includes('KOMO_WORLD_MOSAIC_V51')&&runtime.includes('makeKomoWorldMosaicTexture')&&runtime.includes("g.fillText('KŌMØ'")&&runtime.includes("g.fillText('W O R L D'")],
  ['flagship brand wall V5.1 present',runtime.includes('KOMO_FLAGSHIP_BRAND_V51')&&runtime.includes("'WORLD · LONGEVITY IN MOTION'")&&runtime.includes("'LONGEVITY','LIVES HERE'")],
  ['flagship lobby decor V5.1 present',runtime.includes('KOMO_FLAGSHIP_DECOR_V51')&&runtime.includes('function flagshipTree')&&runtime.includes('new THREE.TorusGeometry(.82,.055')],
  ['V6.1 grand flagship present',runtime.includes('KOMO_GRAND_FLAGSHIP_V61')&&runtime.includes('KOMO_GRAND_STONE_FINS_INST_V61')&&runtime.includes('KOMO_GRAND_CEILING_RIBS_INST_V61')],
  ['V6.1 monumental armillary present',runtime.includes('KOMO_ATRIUM_ARMILLARY_V61')&&runtime.includes('grandRingA.rotation.y')&&runtime.includes('grandCoreV61.scale.setScalar')],
  ['V6.1 destination theatre present',runtime.includes('KOMO_DESTINATION_THEATRE_V61')&&runtime.includes("i===0?'TWIN':i===1?'FITNESS':'ARENA'")],
  ['V6.2 widened room promenades present',runtime.includes('campusPath(25.8,7.1,-22.0,-24.0)')&&runtime.includes('campusPath(9.4,16.8,0,-35.5)')&&runtime.includes('WALK IN')],
  ['V6.2 covered access architecture present',runtime.includes('KOMO_ACCESS_ARCHITECTURE_V62')&&runtime.includes('KOMO_ACCESS_ROOF_INST_V62')&&runtime.includes('KOMO_ACCESS_BEAM_INST_V62')&&runtime.includes("'OPEN GALLERY'")],
  ['V6.2 access LOD present',runtime.includes("accessArchitectureV62.visible=nearAccess")&&runtime.includes("if(typeof accessArchitectureV62!=='undefined')accessArchitectureV62.visible=false")],

  ['V6.1 grandeur performance budget present',runtime.includes("grandFlagshipV61.visible=!emergencyPerformance")&&runtime.includes("destinationTheatreV61.visible=!emergencyPerformance")&&runtime.includes('instancedStatic(grandFlagshipV61')],

  ['permanent auto-run V5.3 present',runtime.includes('const AUTO_RUN_SPEED=lowPower?6.05:7.05')&&runtime.includes('const AUTO_RUN_BOOST=lowPower?7.15:8.35')&&runtime.includes('tryMoveSmooth')&&runtime.includes("BOOST (COURSE)"),],
  ['V7 running animation present',runtime.includes('const strideAmp=THREE.MathUtils.lerp(.34,.58,runBlend)*moveAmount')&&runtime.includes('av.leftKnee.rotation.x=Math.max(0,-s)*kneeAmp')&&runtime.includes('av.torsoGroup.rotation.x=-THREE.MathUtils.lerp(.004,.038,runBlend)*moveAmount')],
  ['fluid run camera V6.9 present',runtime.includes('const runAmount=THREE.MathUtils.clamp(velocity.length()/AUTO_RUN_SPEED')&&runtime.includes('const desiredFov=50+2.4*runAmount')&&runtime.includes('thirdPersonDistance+(lowPower?.10:.34)*runAmount')],
  ['walkable room access V5.8 present',runtime.includes('KOMO_ROOM_ACCESS_V52')&&runtime.includes('KOMO_ROOM_RETURN_')&&runtime.includes('function requestWalkTo')&&runtime.includes('function getCampusZone')&&runtime.includes('manualCampusDestination')&&runtime.includes('function updateRoomAccess(now)')&&runtime.includes('updateRoomAccess(now);')&&!runtime.includes('function seamlessRoomTransition')],
  ['NPC missions V5.4 present',runtime.includes("const NPC_MISSION_KEY='komo_world_npc_missions_v1'")&&runtime.includes("maya:{name:'Maya'")&&runtime.includes("jules:{name:'Jules'")&&runtime.includes('function showNpcMission')&&runtime.includes('function registerNpcTalk')],
  ['Daily Health bridge V5.4 present',runtime.includes("const DAILY_HEALTH_KEY='komo_world_daily_health_v1'")&&runtime.includes("const HEALTH_BRIDGE_CHANNEL='komo-health-v1'")&&runtime.includes('function ingestDailyHealth')&&runtime.includes('function showDailyHealthConnect')&&runtime.includes('getDailyHealth:()=>')],
  ['Pulse Daily Health hydration V5.4 present',multiplayer.includes("from('pulse_daily_health')")&&multiplayer.includes("runtime.ingestDailyHealth?.(")],
  ['World entry gateway V5.5 present',html.includes('id="intro-pulse"')&&html.includes('id="intro-guest-name"')&&runtime.includes('function enterWorldAfterAuth')&&runtime.includes("'komo:world-session-ready'")],
  ['Open premium rooms V5.6 present',runtime.includes("KOMO_OPEN_ROOMS_V56")&&runtime.includes("KOMO_FITNESS_PREMIUM_V56")&&runtime.includes("KOMO_TWIN_PREMIUM_V56")&&runtime.includes("KOMO_ARENA_PREMIUM_V56")],
  ['Living campus sightlines V5.7 present',runtime.includes('KOMO_OPEN_ATRIUM_WALL_V57')&&runtime.includes('KOMO_DESTINATION_VISTAS_V57')&&runtime.includes('function vistaTunnel')&&runtime.includes('function portalActor')],
  ['Living room animation V5.7 present',runtime.includes('fitnessStatusBars')&&runtime.includes('twinCeilingHalo.rotation.z')&&runtime.includes('arenaCanopy.rotation.z')&&runtime.includes('portalActors.forEach')],
  ['Destination rooms permanently open V5.6',runtime.includes("destination rooms are permanently open")&&runtime.includes("d.progress+=(1-d.progress)")],
  ['Resilient guest entry V1.4 present',multiplayer.includes('signInAnonymously')&&multiplayer.includes('const connectGuest=async')&&multiplayer.includes('const startLocalGuest=async')&&multiplayer.includes("version:'1.4.0-oversized-avatar'")&&multiplayer.includes("emitSessionReady('guest-local')")],
  ['Twin room V5.2 present',runtime.includes('KOMO_TWIN_ROOM_V52')&&runtime.includes('Data canopy')===false&&runtime.includes('new THREE.TorusGeometry(3.0+i*.34')],
  ['Fitness room V5.2 present',runtime.includes('KOMO_FITNESS_ROOM_V52')&&runtime.includes("'TODAY','MOVE · TRAIN · RECOVER'")&&runtime.includes('MAT.smokedGlass')],
  ['Arena room V5.2 present',runtime.includes('KOMO_ARENA_ROOM_V52')&&runtime.includes("'LIVE ARENA','DAILY CHALLENGES · SOCIAL'")&&runtime.includes('new THREE.RingGeometry(2.05,2.18,64)')],
  ['destination access V5.6 present',runtime.includes('left.position.x=-2.20;right.position.x=2.20')&&runtime.includes('progress:1')&&runtime.includes('d.progress+=(1-d.progress)')&&runtime.includes('doorTarget?11.2:4.6')],
  ['continuous navigation mesh V6.2 present',runtime.includes('Broad overlapping galleries make each room part of one continuous navigation mesh')&&runtime.includes('p.x>-57.2&&p.x<-32.15')&&runtime.includes('p.z>-45.5&&p.z<-26.25')&&runtime.includes('p.x>32.15&&p.x<57.2')],
  ['walkable guide routing V5.8 present',runtime.includes('function campusWalkingTarget')&&runtime.includes("manualCampusDestination==='")===false&&runtime.includes('if(manualCampusDestination)')&&runtime.includes("requestWalkTo('hall')")],
  ['lateral return arches V5.8 present',runtime.includes("x:12.72,z:-2.10,rot:-Math.PI/2")&&runtime.includes("x:-12.72,z:-2.10,rot:Math.PI/2")],
  ['cross-device optical depth V6.9 present',runtime.includes('scene.fog=new THREE.Fog(0xd4d9d1,82,270)')&&runtime.includes("new THREE.PerspectiveCamera(54")],
  ['desktop V5 performance safety present',runtime.includes("desktopCinematic.visible=false")&&runtime.includes("desktopCinematic.visible=!lowPower&&!emergencyPerformance")],
  ['desktop UI V5 present',css.includes('V5.0 Desktop Visual Revolution')&&css.includes('body.desktop-visual-v5 #world-canvas')&&css.includes('body.desktop-visual-v5::before')&&css.includes('body.desktop-visual-v5 .world-hud')],
  ['desktop UI width-gated V6.9',css.includes('@media(min-width:901px) and (pointer:fine)')&&runtime.includes("classList.toggle('desktop-visual-v5',innerWidth>900)")],
  ['V7 avatar visible-floor grounding present',runtime.includes('AVATAR_SOLE_COMPENSATION=.043')&&runtime.includes('function visualSurfaceOffsetAt')&&runtime.includes('KOMO_PLAYER_BODY_GROUNDING_V70')&&runtime.includes('av.shadow.position.y=surfaceOffset+.004')],
  ['camera follows visible floor V5.0.1 present',runtime.includes('const visualGround=visualSurfaceOffsetAt(player)')&&runtime.includes('player.y+visualGround+height')&&runtime.includes('player.y+visualGround+2.03+bob')],
  ['NPC visible-floor grounding V5.0.1 present',runtime.includes('KOMO_NPC_BODY_GROUNDING_V501')&&runtime.includes('const npcLift=Math.max(0,npcSurface-.169)')],
  ['V7.1 remote visible-floor grounding present',multiplayer.includes('KOMO_REMOTE_BODY_GROUNDING_V71')&&multiplayer.includes('runtime.getAvatarGroundLift?.(peer.position)')&&multiplayer.includes('av.ring.position.y=remoteSurface+.008')],
  ['mobile UI V4.6 present',css.includes('V4.6 Mobile UI')&&css.includes('body.world-intro-active .mobile-controls')&&css.includes('.journey-hud,.health-hud,.location-chip')&&css.includes('100dvh')],
  ['mobile onboarding V4.6 state present',runtime.includes("document.body.classList.add('world-intro-active')")&&runtime.includes("document.body.classList.remove('world-intro-active')")&&runtime.includes("world-mobile-ui")],
  ['mobile social V0.9.0 present',multiplayer.includes('V4.6 Mobile social layer')&&multiplayer.includes("world-intro-active")&&multiplayer.includes("world-social-open")&&multiplayer.includes("max-height:min(52dvh,460px)")],
  ['mobile voice compact V0.9.0 present',multiplayer.includes('width:46px!important')&&multiplayer.includes('.kwmp-talk:before{content:"🎙"')&&multiplayer.includes("voicebox.classList.add('collapsed')")],
  ['persistent Minecraft-style chat present',multiplayer.includes("drawer.className='kwmp-chat'")&&multiplayer.includes("const openChat=()=>")&&multiplayer.includes("data-kwmp-target")],
  ['private messages present',multiplayer.includes("recipient_id")&&multiplayer.includes("setDmTarget")&&multiplayer.includes("dm.textContent='MP'")],
  ['community roles present',multiplayer.includes("komo_community_roles")&&multiplayer.includes("role_title")&&multiplayer.includes("display_title")],
  ['social communication XP present',multiplayer.includes("SOCIAL_KEY")&&multiplayer.includes("awardSocial")&&multiplayer.includes("runtime.completeSocial?.()")],
  ['daily social missions V0.9 present',multiplayer.includes('SOCIAL_MISSIONS')&&multiplayer.includes('BRISER LA GLACE')&&multiplayer.includes('BOUGER ENSEMBLE')&&multiplayer.includes('kwmp-social-missions')],
  ['social icebreakers V0.9 present',multiplayer.includes('data-kwmp-prompt')&&multiplayer.includes('DÉFI ARENA')&&multiplayer.includes('EXPLORER LE TWIN')&&multiplayer.includes("wave.textContent='SALUER'")],
  ['social join challenge V0.9 present',multiplayer.includes("awardSocial('join',15)")&&runtime.includes("id:'social_join'")&&runtime.includes("join:'social_join'")],
  ['social proximity challenge V0.9 present',multiplayer.includes('closestDist<=6')&&multiplayer.includes("addSocialProgress('together',seconds)")&&runtime.includes("id:'social_together'")],
  ['social voice challenge V0.9 present',multiplayer.includes("state.voice.connected.size>0)awardSocial('voice',20)")&&runtime.includes("id:'social_voice'")],
  ['social challenge bridge V4.7 present',runtime.includes('function socialChallengeEvent')&&runtime.includes('socialChallenge:socialChallengeEvent')&&runtime.includes('getChallengeState:()=>')],
  ['proximity voice present',multiplayer.includes("getUserMedia")&&multiplayer.includes("RTCPeerConnection")&&multiplayer.includes("VOICE_ENTER_M=18")&&multiplayer.includes("world_voice_signals")],
  ['horizon sun disc removed',runtime.includes("V3.4 horizon cleanup")&&runtime.includes("living.sunSprite=null")],
  ['multiplayer clock-skew safety V0.5.3 present',multiplayer.includes('const CLOCK_SKEW_MS=180000')&&multiplayer.includes('timestampPlausible')&&multiplayer.includes('_local_seen_at')],
  ['multiplayer heartbeat verifies writes',multiplayer.includes("const {error}=await client.from('world_presence').upsert")&&multiplayer.includes("state.presenceLive=true")],
  ['multiplayer mobile presence tolerance',multiplayer.includes('const STALE_MS=45000')&&multiplayer.includes('const HEARTBEAT_MS=2200')],
  ['multiplayer roster present',multiplayer.includes('const renderRoster=()=>')&&multiplayer.includes('kwmpRoster')],
  ['multiplayer Safari restore present',multiplayer.includes('client.auth.getSession().then')&&multiplayer.includes('event.persisted')],
  ['Pulse cross-tab World bridge present',pulseAuth.includes('WORLD_BRIDGE_CHANNEL')&&pulseAuth.includes("type:'komo:pulse-world-session-request'")&&pulseAuth.includes("type:'komo:pulse-world-session-response'")],
  ['World bridge ACK present',pulseAuth.includes("type!=='komo:world-bridge-ack'")&&multiplayer.includes("type:'komo:world-bridge-ack'")],
  ['Pulse refresh-token race fixed',multiplayer.includes('World consumes Pulse access tokens but never rotates its refresh token')&&multiplayer.includes("autoRefreshToken:false")&&multiplayer.includes('expiresAt*1000<Date.now()+15000')],
  ['World never globally signs out Pulse',!multiplayer.includes('await client.auth.signOut()')&&!multiplayer.includes("signOut({ scope: 'global'")&&multiplayer.includes('Never sign out a bridged Pulse session')],
  ['World auth error feedback present',multiplayer.includes("komo:world-auth-error")&&runtime.includes("window.addEventListener('komo:world-auth-error'")&&runtime.includes("value==='pending'")],
  ['Guest entry never blocks present',runtime.includes("enterWorldAfterAuth({mode:'guest-local'")&&runtime.includes("waitForWorldMultiplayer(12000).then(api=>api.connectGuest(name))")],
  ['Pulse popup user gesture preserved',runtime.includes("Never await before opening Pulse")&&multiplayer.includes("Must open synchronously from the click event")],
  ['Pulse bridge retries until ACK',pulseAuth.includes('worldBridgeRetryTimer=setInterval')&&pulseAuth.includes('Date.now()-worldBridgeLastSendAt<850')&&pulseAuth.includes('if(worldBridgeAcked)return')],
  ['Pulse bridge runtime loaded',pulseHtml.includes('auth-gateway-v2.js?v=20260924-world-bridge-v3')],
  ['Pulse bridge stores retry payload',pulseAuth.includes('worldBridgeLastPayload=payload')&&pulseAuth.includes('if(worldBridgeLastPayload&&!worldBridgeAcked)')],


  ['multiplayer join friend V0.5 present',runtime.includes('function joinPresence(target)')&&runtime.includes('joinPresence,')&&multiplayer.includes('runtime.joinPresence?.(row)')],
  ['multiplayer distance roster present',multiplayer.includes("Math.round(distance)+' m away'")&&multiplayer.includes("join.textContent='JOIN'")],
  ['multiplayer long-range beacon present',multiplayer.includes('distance<90')&&multiplayer.includes('beaconTop')&&multiplayer.includes('depthTest:false')],
  ['configurable keyboard controls present',runtime.includes("const KEYBIND_KEY='komo_world_keybinds_v1'")&&runtime.includes('function showControlsPanel')&&runtime.includes("controlsToggle.addEventListener")],
  ['movement uses configured controls',runtime.includes("if(isPressed('forward'))")&&runtime.includes("keyHas('action',e.code)")&&runtime.includes("keyHas('menu',e.code)")],
  ['multiplayer live motion V1.1 present',multiplayer.includes('const POSE_MS=250')&&multiplayer.includes("event:'pose'")&&multiplayer.includes(".on('broadcast',{event:'pose'}")],
  ['multiplayer live interpolation present',multiplayer.includes('lerp(peer.userData.target,.28)')&&multiplayer.includes('peer.rotation.y+=d*.26')],
  ['multiplayer symmetric Presence V0.5.2 present',multiplayer.includes("presence:{key:state.session.user.id}")&&multiplayer.includes(".on('presence',{event:'sync'}")&&multiplayer.includes(".on('presence',{event:'join'}")&&multiplayer.includes(".on('presence',{event:'leave'}")],
  ['multiplayer fallback refresh present',multiplayer.includes('const PRESENCE_REFRESH_MS=4000')&&multiplayer.includes('const refreshPresence=async')&&multiplayer.includes("U.people.addEventListener('click',async()=>{await refreshPresence()")]
];
for(const [label,ok] of checks){
  console.log(`[komo-world-qa] ${ok?'OK':'FAIL'} · ${label}`);
  if(!ok)process.exit(1);
}

await rm(target, { recursive: true, force: true });
await mkdir(target, { recursive: true });
await cp(source, target, { recursive: true });

console.log(`[komo-world-qa] PASS · ${checks.length}/${checks.length} · canonical World copied to /site/world/`);
