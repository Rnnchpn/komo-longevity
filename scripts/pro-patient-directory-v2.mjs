import {readFile,writeFile} from 'node:fs/promises';
const path='pulse-app/pro-followup-v1.js';
let src=await readFile(path,'utf8');
src=src.replace("const S={client:null,session:null,role:'member',active:false,rows:[],requests:[],loading:false,error:null,orgId:localStorage.getItem(ORG_KEY)||''};","const S={client:null,session:null,role:'member',active:false,rows:[],requests:[],loading:false,error:null,orgId:localStorage.getItem(ORG_KEY)||'',visibility:'assigned',memberships:[],search:''};");
if(!src.includes('function visibilityTitle()')){
  src=src.replace("function motionLabel(v){return({baseline:'À démarrer',collecting:'Mesures en cours',review:'À revoir',validated:'Validé',released:'Publié',draft:'Brouillon'})[v]||v||'Pas de bilan'}","function motionLabel(v){return({baseline:'À démarrer',collecting:'Mesures en cours',review:'À revoir',validated:'Validé',released:'Publié',draft:'Brouillon'})[v]||v||'Pas de bilan'}\nfunction visibilityTitle(){return S.visibility==='global'?'Tous les patients':S.visibility==='center'?'Patients du centre':'Mes patients'}\nfunction visibilityCopy(){return S.visibility==='global'?'Vue globale de tous les dossiers patients accessibles à KŌMØ Admin.':S.visibility==='center'?'Tous les patients du centre sélectionné, avec leur préparation, leurs demandes et leurs rendez-vous.':'Tous les patients qui vous sont affectés, réunis dans une seule liste.'}\nfunction filterRows(rows){const q=S.search.trim().toLowerCase();if(!q)return rows;return rows.filter(x=>{const p=x.patient||{};return [patientName(p),p.email,p.external_reference,p.organizations?.name].filter(Boolean).join(' ').toLowerCase().includes(q)})}");
}
src=src.replace("return S.orgId?S.rows.filter(x=>x.patient?.organization_id===S.orgId):S.rows}","const rows=S.orgId?S.rows.filter(x=>x.patient?.organization_id===S.orgId):S.rows;return filterRows(rows)}");
src=src.replace("S.rows=dash.data?.rows||[];S.requests=req.data||[];organizations();selectedRows()","S.rows=dash.data?.rows||[];S.visibility=dash.data?.visibility||'assigned';S.memberships=dash.data?.memberships||[];S.requests=req.data||[];organizations();selectedRows()");
src=src.replace('<h2>Mes patients.</h2><p>Préparation, demandes, bilan et prochaine consultation réunis dans une seule vue.</p>','<h2>${esc(visibilityTitle())}.</h2><p>${esc(visibilityCopy())}</p>');
src=src.replace('<div class="kfollow-head-actions"><label class="kfollow-center"><span>Centre</span>','<div class="kfollow-head-actions"><label class="kfollow-search"><span>Rechercher</span><input id="kfollowSearch" type="search" value="${esc(S.search)}" placeholder="Nom, e-mail, référence…"></label><label class="kfollow-center"><span>Centre</span>');
src=src.replace('<div><span>Mes patients</span><strong>${rows.length}</strong></div>','<div><span>${esc(visibilityTitle())}</span><strong>${rows.length}</strong></div>');
src=src.replace("function bind(){document.querySelector('#kfollowRefresh')?.addEventListener('click',load);","function bind(){document.querySelector('#kfollowSearch')?.addEventListener('input',e=>{S.search=e.target.value;render()});document.querySelector('#kfollowRefresh')?.addEventListener('click',load);");
await writeFile(path,src);
console.log('[pro-patient-directory-v2] patient visibility labels, complete authorized directory and search applied');
