import { readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const roots=['pulse-app',join('site','pulse-v12')];

function mustReplace(src,re,replacement,label){
  if(re.test(src))return src.replace(re,replacement);
  if(typeof replacement==='string'&&src.includes(replacement))return src;
  throw new Error(`[pulse-motion-domains] contract changed: ${label}`);
}

function patchPayload(src){
  const defs=`const FUNCTIONAL_DEFS=[
  ['M-FUN-01','GLFS-25','points'],['M-FUN-04','Chair Stand 30 s','rép.'],['M-FUN-03','Two-Step','ratio']
];
const QUALITY_OF_LIFE_DEFS=[
  ['KOMO_SLEEP_RECOVERY','Sommeil & récupération'],['KOMO_WELLBEING','Énergie & bien-être'],['KOMO_LIFESTYLE','Mode de vie']
];`;
  src=mustReplace(src,/const FUNCTIONAL_DEFS=\[[\s\S]*?\];/,defs,'functional definitions');

  if(!src.includes('function qualityOfLifeSummary(qs)')){
    const helper=`function qualityOfLifeSummary(qs){const items=QUALITY_OF_LIFE_DEFS.map(([instrumentCode,label])=>{const q=qs.find(x=>x.instrumentCode===instrumentCode)||null;return{instrumentCode,label,status:q?.status||'not_started',score:q?.score??null,scoreStatus:q?.scoreStatus||'not_scored',completeness:q?.completeness??0,responseCount:q?.responseCount??0,completed:!!q&&q.status==='completed'&&Number(q.completeness||0)>=100,numericContribution:0}});return{items,completedCount:items.filter(x=>x.completed).length,totalCount:items.length,numericContribution:0,role:'quality_of_life_daily'}}\n`;
    src=mustReplace(src,/function measurementValue\(m\)\{/,helper+'function measurementValue(m){','quality of life helper');
  }

  const posture=`function postureSummary(d){const ms=d?.measurements||[],defs=[['M-POS-01','Inclinaison du tronc','°'],['M-POS-02','SVA','mm']];const items=defs.map(([code,label,unit])=>{const m=ms.filter(x=>x.indicator_code===code&&measurementValue(x)!==null&&measurementValue(x)!=='').sort((a,b)=>new Date(b.recorded_at||0)-new Date(a.recorded_at||0))[0]||null;const value=m?measurementValue(m):null;return{code,label,value,unit:m?.unit||unit,source:m?.source||'',protocolVersion:m?.protocol_version||d?.motion?.protocol_version||'',qcStatus:m?.qc_status||'',recordedAt:m?.recorded_at||null,available:value!==null&&value!==''}});const sva=items.find(x=>x.code==='M-POS-02'&&x.available)||null;return{svaMm:sva?round(n(sva.value),1):null,unit:sva?.unit||'mm',source:sva?.source||'',protocolVersion:sva?.protocolVersion||d?.motion?.protocol_version||'',qcStatus:sva?.qcStatus||'',recordedAt:sva?.recordedAt||null,contextClass:d?.motion?.context_class||'',assessmentStatus:d?.motion?.status||'',items,available:items.some(x=>x.available),numericContribution:0}}`;
  src=mustReplace(src,/function postureSummary\(d\)\{[\s\S]*?\}\nfunction lsiRows/,posture+'\nfunction lsiRows','posture summary');

  src=src.replace('questionnaires=questionnaireSessions(d),functional=functionalSummary(d,questionnaires),posture=postureSummary(d)', 'questionnaires=questionnaireSessions(d),functional=functionalSummary(d,questionnaires),qualityOfLife=qualityOfLifeSummary(questionnaires),posture=postureSummary(d)');
  if(!src.includes('qualityOfLife=qualityOfLifeSummary(questionnaires)'))throw new Error('[pulse-motion-domains] payload quality of life binding missing');
  src=src.replace(',muscle,posture,context:{',',muscle,posture,qualityOfLife,context:{');
  if(!src.includes(',qualityOfLife,context:{'))throw new Error('[pulse-motion-domains] payload quality of life output missing');
  src=src.replace("note:'Le pré-bilan et les questionnaires décrivent le contexte du patient. Ils ne modifient pas le Motion Score.'", "note:'Les questionnaires complets restent tracés. La restitution Motion distingue le GLFS-25 fonctionnel de la qualité de vie quotidienne.'");
  src=src.replace("scorePolicy:'sensor_only',", "scorePolicy:'sensor_only',resultArchitectureVersion:'motion-five-result-families-v1',resultDomains:['functional','muscle','gait','posture','quality_of_life_daily'],");
  src=src.replace('questionnaireContribution:0,functionalContribution:0,postureContribution:0,gaitContribution:0', 'questionnaireContribution:0,functionalContribution:0,postureContribution:0,gaitContribution:0,qualityOfLifeContribution:0');
  src=src.replaceAll('functional.filter(x=>x.available).length<7','functional.filter(x=>x.available).length<3');
  return src;
}

function patchResults(src){
  const hero=`function motionHero(result,payload,findings){const s=payload.summary||{},fn=payload.function||{},sensor=payload.sensor||{},muscle=payload.muscle||{},posture=payload.posture||{},quality=payload.qualityOfLife||{},heroFinding=findings.get('neuromuscular_symmetry'),heroStatus=heroFinding?.status||'descriptive';const wording=heroFinding?.patientMessage||s.sentence||'Votre bilan Motion constitue votre référence instrumentée.';const muscleReady=(sensor.lsi||[]).filter(x=>n(x.value)!==null).length;return \`<article class="kr4-card kr4-hero"><div class="kr4-hero-main"><div><div class="kr4-hero-top"><div><div class="kr4-kicker">RÉSULTAT MOTION</div><div class="kr4-score">\${score(s.score)}<small>/100</small></div></div>\${statusChip(heroStatus,statusLabel(s.releaseStatus))}</div><div class="kr4-headline"><h2>\${esc(heroFinding?.status==='favorable'?'Votre mouvement est dans une zone favorable.':heroFinding?.status==='priority'?'Une priorité ressort clairement.':heroFinding?.status==='watch'?'Un point mérite votre attention.':'Votre référence Motion.')}</h2><p>\${esc(wording)}</p></div></div><div class="kr4-actions"><button class="kr4-btn primary" type="button" data-komo-export-report>Exporter le rapport complet</button><button class="kr4-btn" type="button" data-scroll-clinical>Voir Clinical</button></div></div><aside class="kr4-hero-side"><div class="kr4-stat"><span>Fonctionnel</span><strong>\${fn.availableCount??0}/\${fn.totalCount??3}</strong><small>GLFS-25 · Chair 30 s · Two-Step</small></div><div class="kr4-stat"><span>Muscle</span><strong>\${muscleReady}/3</strong><small>Quadriceps · ischio-jambiers · mollets</small></div><div class="kr4-stat"><span>Marche</span><strong>\${sensor.gait?.scalarCountPresent??0}/\${sensor.gait?.scalarCountExpected??15}</strong><small>Profil spatio-temporel</small></div><div class="kr4-stat"><span>Posture</span><strong>\${posture.available?'Disponible':'—'}</strong><small>Alignement mesuré</small></div><div class="kr4-stat"><span>Qualité de vie</span><strong>\${quality.completedCount??0}/\${quality.totalCount??3}</strong><small>Sommeil · énergie · mode de vie</small></div></aside></article>\`}`;
  src=mustReplace(src,/function motionHero\(result,payload,findings\)\{[\s\S]*?\}\n\nfunction summarySection/,hero+'\n\nfunction summarySection','Motion hero');

  const functional=`function functionalSection(payload){const tests=payload.function?.tests||[];return \`<article class="kr4-card kr4-section"><div class="kr4-section-head"><div><div class="kr4-kicker">RÉSULTATS FONCTIONNELS</div><h3>Fonction, autonomie et performance.</h3></div><p>Trois repères composent la restitution fonctionnelle : GLFS-25, Chair Stand 30 secondes et Two-Step.</p></div>\${tests.length?\`<div class="kr4-rowlist">\${tests.map(r=>{const t=r.available?qcTone(r.qcStatus):'neutral';const val=r.available?(typeof r.value==='number'?display(r.value,r.unit||'',r.code==='M-FUN-03'?2:1):\`\${valueText(r.value)}\${r.unit?\` \${r.unit}\`:''}\`):'Non recueilli';return \`<article class="kr4-row \${t}"><div><strong>\${esc(r.label)}</strong><small>\${esc(r.code)} · \${esc(fmtDate(r.recordedAt))}</small></div><div class="kr4-value">\${esc(val)}</div><div class="kr4-source">\${esc(r.source||'—')}<br><small>Résultat fonctionnel KŌMØ</small></div>\${r.available?qcChip(r.qcStatus):statusChip('descriptive','Non recueilli')}</article>\`}).join('')}</div>\`:'<div class="kr4-empty">Aucun résultat fonctionnel enregistré.</div>'}</article>\`}`;
  src=mustReplace(src,/function functionalSection\(payload\)\{[\s\S]*?\}\n\nfunction muscleSection/,functional+'\n\nfunction muscleSection','functional section');

  const posture=`function postureSection(payload){const p=payload.posture||{},items=(p.items||[]).filter(x=>x.available);return \`<article class="kr4-card kr4-section"><div class="kr4-section-head"><div><div class="kr4-kicker">POSTURE</div><h3>Votre alignement mesuré.</h3></div><p>Les mesures posturales Motion restent distinctes de la posture clinique complète, réservée à Clinical.</p></div>\${items.length?\`<div class="kr4-grid">\${items.map(x=>metricSignal(x.label,typeof x.value==='number'?display(x.value,x.unit||'',1):valueText(x.value),\`\${x.source||'Source enregistrée'} · \${fmtDate(x.recordedAt)}\`,x.qcStatus==='valid'?'favorable':x.qcStatus==='suspect'?'watch':x.qcStatus==='invalid'?'priority':'descriptive')).join('')}</div>\`:'<div class="kr4-empty">Aucune mesure posturale publiée dans ce bilan.</div>'}</article>\`}`;
  src=mustReplace(src,/function postureSection\(payload\)\{[\s\S]*?\}\n\nfunction questionnaireStatus/,posture+'\n\nfunction questionnaireStatus','posture section');

  const quality=`function qualityOfLifeSection(payload){const q=payload.qualityOfLife||{},items=q.items||[];return \`<article class="kr4-card kr4-section"><div class="kr4-section-head"><div><div class="kr4-kicker">QUALITÉ DE VIE & QUOTIDIEN</div><h3>Ce que votre quotidien raconte.</h3></div><p>Sommeil & récupération, énergie & bien-être et mode de vie complètent la mesure instrumentée sans dupliquer les données Connected.</p></div><div class="kr4-grid">\${items.length?items.map(x=>{const completed=!!x.completed;const value=x.score!==null&&x.score!==undefined?\`Score \${valueText(x.score)}\`:\`\${Math.round(n(x.completeness)||0)} %\`;return metricSignal(x.label,value,completed?'Questionnaire complété':'À compléter','descriptive')}).join(''):'<div class="kr4-empty">Aucune donnée de qualité de vie disponible.</div>'}</div><p class="kr4-note">Les réponses détaillées restent dans le dossier technique ; ce bloc restitue uniquement les dimensions utiles à la trajectoire quotidienne.</p></article>\`}`;
  src=mustReplace(src,/function questionnaireStatus\(q\)\{[\s\S]*?\}\nfunction questionnaireSection\(payload\)\{[\s\S]*?\}\n\nfunction prioritiesSection/,quality+'\n\nfunction prioritiesSection','quality of life section');
  src=src.replace('${questionnaireSection(payload)}','${qualityOfLifeSection(payload)}');
  if(!src.includes('${qualityOfLifeSection(payload)}'))throw new Error('[pulse-motion-domains] Results quality of life render missing');

  const clinical=`async function loadClinical(client,patientId){if(!patientId)return{assessments:[],reports:[],contexts:[]};const a=await client.from('assessments').select('id,status,created_at').eq('patient_id',patientId).eq('product_mode','clinical').order('created_at',{ascending:false}).limit(8);if(a.error||!(a.data||[]).length)return{assessments:[],reports:[],contexts:[]};const ids=a.data.map(x=>x.id);const [rq,cq]=await Promise.all([client.from('reports').select('assessment_id,version,report_type,status,content_manifest,released_at').in('assessment_id',ids).eq('status','released').order('released_at',{ascending:false}),client.from('clinical_context').select('assessment_id,examination,biology,imaging,medical_conclusion,status,signed_at').in('assessment_id',ids).eq('status','signed').order('signed_at',{ascending:false})]);return{assessments:a.data||[],reports:rq.error?[]:rq.data||[],contexts:cq.error?[]:cq.data||[]}}\nfunction clinicalManifestText(m){if(!m||typeof m!=='object')return'';const candidates=[m.medical_conclusion,m.medicalConclusion,m.conclusion,m.summary,m.patient_summary,m.patientSummary,m.title].filter(x=>typeof x==='string'&&x.trim());return candidates[0]||''}\nfunction clinicalLabel(v=''){return String(v||'').replace(/[_-]+/g,' ').replace(/\\b\\w/g,c=>c.toUpperCase())}\nfunction clinicalObjectRows(obj,prefix='',depth=0){if(!obj||typeof obj!=='object'||depth>2)return[];const out=[];for(const [k,v] of Object.entries(obj)){const label=prefix?\`\${prefix} · \${clinicalLabel(k)}\`:clinicalLabel(k);if(v===null||v===undefined||v==='')continue;if(Array.isArray(v)){if(v.every(x=>['string','number','boolean'].includes(typeof x)))out.push([label,v.map(valueText).join(', ')]);continue}if(typeof v==='object'){if('value'in v){const unit=v.unit?\` \${v.unit}\`:'';out.push([label,\`\${valueText(v.value)}\${unit}\`])}else out.push(...clinicalObjectRows(v,label,depth+1));continue}out.push([label,valueText(v)])}return out}\nfunction clinicalPostureRows(contexts){const keys=/postur|sva|sagitt|align|lordos|kypho|pelv|rachis|spine/i;const rows=[];for(const c of contexts||[]){rows.push(...clinicalObjectRows(c.examination,'Examen').filter(([k])=>keys.test(k)),...clinicalObjectRows(c.imaging,'Imagerie').filter(([k])=>keys.test(k)))}return rows.slice(0,10)}\nfunction clinicalBiologyRows(contexts){const rows=[];for(const c of contexts||[])rows.push(...clinicalObjectRows(c.biology));return rows.slice(0,12)}\nfunction clinicalRowsHtml(rows,empty){return rows.length?\`<div class="kr4-rowlist">\${rows.map(([k,v])=>\`<div class="kr4-row"><strong>\${esc(k)}</strong><div class="kr4-source" style="grid-column:2/-1">\${esc(v)}</div></div>\`).join('')}</div>\`:\`<p>\${esc(empty)}</p>\`}\nfunction clinicalSection(clinical){const reports=clinical?.reports||[],contexts=clinical?.contexts||[],posture=clinicalPostureRows(contexts),biology=clinicalBiologyRows(contexts),has=reports.length||posture.length||biology.length;return \`<article class="kr4-card kr4-clinical" id="clinicalResults"><div class="kr4-clinical-hero"><div><div class="kr4-kicker">RÉSULTAT CLINICAL</div><h2>Motion, puis la couche médicale.</h2><p>Clinical reprend les cinq familles Motion ci-dessus et ajoute, lorsqu'elles sont validées et signées, la posture complète et la biologie. Clinical n'écrase pas le Motion Score.</p></div>\${has?statusChip('favorable','Données signées'):statusChip('descriptive','En attente')}</div><div class="kr4-clinical-list"><article class="kr4-clinical-card"><div class="kr4-kicker">POSTURE COMPLÈTE</div><strong>Analyse posturale clinique</strong>\${clinicalRowsHtml(posture,'Aucune analyse posturale clinique signée pour le moment.')}</article><article class="kr4-clinical-card"><div class="kr4-kicker">BIOLOGIE</div><strong>Biomarqueurs validés</strong>\${clinicalRowsHtml(biology,'Aucun résultat biologique signé pour le moment.')}</article>\${reports.map(r=>\`<article class="kr4-clinical-card"><div class="kr4-kicker">\${esc(r.report_type||'CLINICAL REPORT')} · V\${esc(r.version??'—')}</div><strong>\${esc(clinicalManifestText(r.content_manifest)||'Compte-rendu Clinical publié')}</strong><p>Publié le \${esc(fmtDate(r.released_at))}. Le détail médical reste celui du compte-rendu validé.</p></article>\`).join('')}</div></article>\`}`;
  src=mustReplace(src,/async function loadClinical\(client,patientId\)\{[\s\S]*?\}\nfunction clinicalManifestText[\s\S]*?\nfunction clinicalSection\(clinical\)\{[\s\S]*?\}\n\nfunction lazySensorRows/,clinical+'\n\nfunction lazySensorRows','Clinical section');

  return src;
}

function patchPdf(src){
  src=src.replace('posture=payload?.posture||{},context=payload?.context||{},method=', 'posture=payload?.posture||{},context=payload?.context||{},quality=payload?.qualityOfLife||{},method=');
  src=src.replace('Une restitution complète de votre bilan : fonction, marche, posture, analyse musculaire, données Myodev, qualité et provenance.', 'Une restitution complète de votre bilan : fonctionnel, muscle, marche, posture et qualité de vie quotidienne, avec le détail technique disponible à la demande.');
  src=src.replace("statusPill(M+7,199,'FONCTION',(fn.availableCount||0)===(fn.totalCount||7));statusPill(M+40,199,'MARCHE',!!sensor.gait?.complete);statusPill(M+73,199,'POSTURE',!!posture.available);statusPill(M+106,199,'MUSCLE',(sensor.lsi||[]).filter(x=>n(x.value)!==null).length===3);statusPill(M+139,199,'CONTEXTE',(context.completedQuestionnaireCount||0)===(context.questionnaireCount||0)&&Number(context.questionnaireCount||0)>0);text(`${fn.availableCount??0}/${fn.totalCount??7} tests fonctionnels · ${sensor.gait?.scalarCountPresent??0}/${sensor.gait?.scalarCountExpected??15} valeurs de marche · ${context.completedQuestionnaireCount??0}/${context.questionnaireCount??0} questionnaires.`,M+7,220,CW-14,5.7,C.muted,3);", "statusPill(M+7,199,'FONCTION',(fn.availableCount||0)===(fn.totalCount||3));statusPill(M+40,199,'MUSCLE',(sensor.lsi||[]).filter(x=>n(x.value)!==null).length===3);statusPill(M+73,199,'MARCHE',!!sensor.gait?.complete);statusPill(M+106,199,'POSTURE',!!posture.available);statusPill(M+139,199,'QUOTIDIEN',(quality.completedCount||0)===(quality.totalCount||3));text(`${fn.availableCount??0}/${fn.totalCount??3} résultats fonctionnels · ${sensor.gait?.scalarCountPresent??0}/${sensor.gait?.scalarCountExpected??15} valeurs de marche · ${quality.completedCount??0}/${quality.totalCount??3} dimensions quotidiennes.`,M+7,220,CW-14,5.7,C.muted,3);");
  src=src.replace("r.code==='M-FUN-03'?2:r.code==='M-FUN-05'?2:1", "r.code==='M-FUN-03'?2:1");
  const qpage=`// 7 — Qualité de vie & quotidien\n beginPage('QUALITÉ DE VIE','Quotidien & récupération.','Sommeil, énergie et mode de vie complètent les résultats Motion sans dupliquer les données Connected.');\n y=78;const qcols=[0,88,126,154];tableHeader(y,qcols,['DIMENSION','STATUT','COMPL.','SCORE']);y+=8;for(const q of quality.items||[]){tableRow(y,22,qcols,[q.label||q.instrumentCode,q.completed?'Complété':'À compléter',\`${Math.round(Number(q.completeness||0))}%\`,q.score===null||q.score===undefined?'—':String(q.score)],{size:5,boldFirst:true});y+=22}box(M,258,CW,19,C.sagePale);text('Ces dimensions décrivent le quotidien. Leur contribution numérique au Motion Score v0.6 reste à 0 tant que la pondération multidomaine n’est pas validée.',M+7,268,CW-14,5.5,C.deep,3);\n\n // 8 — Agir`;
  src=mustReplace(src,/\/\/ 7 — Questionnaires[\s\S]*?\/\/ 8 — Agir/,qpage,'PDF quality of life page');
  return src;
}

async function patchFile(path,fn){const src=await readFile(path,'utf8');const out=fn(src);await writeFile(path,out,'utf8')}
for(const root of roots){
  await patchFile(join(root,'report-payload-v1.js'),patchPayload);
  await patchFile(join(root,'patient-canonical-results.js'),patchResults);
  await patchFile(join(root,'mobility-report-pdf-v3.js'),patchPdf);
}

// Keep build contracts aligned with the approved product architecture rather than bypassing them.
await patchFile(join('scripts','pulse-report-visual-v2.mjs'),src=>src
  .replace("payload.includes('questionnaireContribution:0')&&payload.includes('functionalContribution:0')&&payload.includes('gaitContribution:0')&&payload.includes('postureContribution:0')", "payload.includes('questionnaireContribution:0')&&payload.includes('functionalContribution:0')&&payload.includes('gaitContribution:0')&&payload.includes('postureContribution:0')&&payload.includes('qualityOfLifeContribution:0')")
  .replace(/\['payload exposes functional context M-FUN-01 to M-FUN-07',[^\n]+\],/, "['payload exposes exactly three official functional results',payload.includes(\"['M-FUN-01','GLFS-25'\")&&payload.includes(\"['M-FUN-04','Chair Stand 30 s'\")&&payload.includes(\"['M-FUN-03','Two-Step'\")&&!payload.includes(\"['M-FUN-02','Stand-Up'\")&&!payload.includes(\"['M-FUN-05','Vitesse de marche 4 m'\")&&!payload.includes(\"['M-FUN-06','Appui unipodal gauche'\")&&!payload.includes(\"['M-FUN-07','Appui unipodal droit'\")&&payload.includes('functional_context_incomplete')],")
  .replace("payload.includes('completedQuestionnaireCount')", "payload.includes('completedQuestionnaireCount')&&payload.includes('qualityOfLife')")
  .replace("renderer.includes('// 7 — Questionnaires')", "renderer.includes('// 7 — Qualité de vie & quotidien')")
  .replace('functional + gait + posture + muscle + questionnaires + full technical traceability','functional + muscle + gait + posture + quality of life + full technical traceability'));
await patchFile(join('scripts','pulse-information-architecture-v11.mjs'),src=>src.replace("results.includes('QUESTIONNAIRES')", "results.includes('QUALITÉ DE VIE & QUOTIDIEN')"));
await patchFile(join('scripts','pulse-runtime-results-consolidation-v31.mjs'),src=>src.replace("'QUESTIONNAIRES','Données Myodev'", "'QUALITÉ DE VIE & QUOTIDIEN','Données Myodev'"));

const payload=await readFile(join('site','pulse-v12','report-payload-v1.js'),'utf8');
const results=await readFile(join('site','pulse-v12','patient-canonical-results.js'),'utf8');
const pdf=await readFile(join('site','pulse-v12','mobility-report-pdf-v3.js'),'utf8');
const checks=[
 ['functional contract is GLFS-25 + Chair 30 s + Two-Step',payload.includes("['M-FUN-01','GLFS-25'")&&payload.includes("['M-FUN-04','Chair Stand 30 s'")&&payload.includes("['M-FUN-03','Two-Step'")&&!payload.includes("['M-FUN-02','Stand-Up'")&&!payload.includes("['M-FUN-05','Vitesse de marche 4 m'")&&!payload.includes("['M-FUN-06','Appui unipodal gauche'")&&!payload.includes("['M-FUN-07','Appui unipodal droit'")],
 ['five Motion result families are explicit',payload.includes("resultDomains:['functional','muscle','gait','posture','quality_of_life_daily']")&&results.includes('QUALITÉ DE VIE & QUOTIDIEN')],
 ['gait speed remains in gait only',payload.includes("['gait_speed_m_s','Vitesse de marche'")],
 ['Motion posture carries measured posture items',payload.includes("['M-POS-01','Inclinaison du tronc'")&&payload.includes("['M-POS-02','SVA'")],
 ['Clinical adds signed full posture and biology',results.includes("from('clinical_context')")&&results.includes("eq('status','signed')")&&results.includes('POSTURE COMPLÈTE')&&results.includes('BIOLOGIE')],
 ['legacy raw measurements remain available',payload.includes('rawMeasurementRows')&&results.includes('TOUTES LES MESURES')],
 ['numeric Motion policy is not silently reweighted',payload.includes("scorePolicy:'sensor_only'")&&payload.includes('qualityOfLifeContribution:0')&&results.includes('Les LSI sont les seules mesures qui alimentent numériquement le Motion Score v0.6')],
 ['PDF follows the five-family architecture',pdf.includes('// 7 — Qualité de vie & quotidien')&&pdf.includes("'QUOTIDIEN'")]
];
for(const [label,ok] of checks){console.log(`[pulse-motion-domains] ${ok?'OK':'FAIL'} · ${label}`);if(!ok)process.exitCode=1}
if(process.exitCode)throw new Error('[pulse-motion-domains] QA failed');
console.log('[pulse-motion-domains] PASS · Motion = Fonctionnel + Muscle + Marche + Posture + Qualité de vie/quotidien · Clinical adds signed full posture + biology');
