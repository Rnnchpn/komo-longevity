import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";
const U=Deno.env.get("SUPABASE_URL")??"",A=Deno.env.get("SUPABASE_ANON_KEY")??"",S=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";
const ORIGINS=new Set(["https://pulse.komolongevity.com","https://komolongevity.com"]),MANAGER_ROLES=new Set(["owner","clinical_admin"]);
function cors(req:Request){const o=req.headers.get("origin")??"";return{"Access-Control-Allow-Origin":ORIGINS.has(o)?o:"https://pulse.komolongevity.com","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Vary":"Origin"}}
function json(req:Request,b:unknown,s=200){return new Response(JSON.stringify(b),{status:s,headers:{...cors(req),"Content-Type":"application/json; charset=utf-8"}})}
function slugify(v:string){return v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,80)||"komo-center"}function clean(v:unknown,n=180){return String(v??"").trim().slice(0,n)}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors(req)});if(req.method!=="POST")return json(req,{error:"method_not_allowed"},405);const token=(req.headers.get("Authorization")??"").replace(/^Bearer\s+/i,"");if(!token)return json(req,{error:"unauthorized"},401);const uc=createClient(U,A,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false}}),svc=createClient(U,S,{auth:{persistSession:false}});const ur=await uc.auth.getUser(token),actor=ur.data?.user;if(ur.error||!actor)return json(req,{error:"unauthorized"},401);const rr=await svc.from("account_roles").select("role").eq("user_id",actor.id).maybeSingle(),accountRole=rr.data?.role??"member",isAdmin=accountRole==="admin";let body:any={};try{body=await req.json()}catch{return json(req,{error:"invalid_json"},400)}const action=String(body.action??"overview");
const memberships=async()=>{const q=await svc.from("organization_members").select("organization_id,role,status,access_scope,organizations(id,name,slug,city,address_line,postal_code,country_code,timezone,status,clinical_data_status,booking_published,contact_email)").eq("user_id",actor.id).eq("status","active");if(q.error)throw new Error(q.error.message);return q.data??[]};const canManage=async(oid:string)=>{if(isAdmin)return true;const ms=await memberships();return ms.some((m:any)=>m.organization_id===oid&&MANAGER_ROLES.has(m.role))};const userEmail=async(id:string)=>{const q=await svc.auth.admin.getUserById(id);return q.data?.user?.email??null};const profile=async(id:string)=>{const q=await svc.from("profiles").select("id,display_name,first_name,last_name,city,country,phone").eq("id",id).maybeSingle();return q.data??null};
if(action==="overview"){if(!["professional","admin"].includes(accountRole))return json(req,{error:"professional_required"},403);const ms=await memberships(),managerIds=new Set(ms.filter((m:any)=>MANAGER_ROLES.has(m.role)).map((m:any)=>m.organization_id));let centers:any[]=[];if(isAdmin){const q=await svc.from("organizations").select("id,name,slug,city,address_line,postal_code,country_code,timezone,status,clinical_data_status,booking_published,contact_email,created_at").order("name");if(q.error)return json(req,{error:"centers_failed",detail:q.error.message},500);centers=q.data??[]}else centers=ms.map((m:any)=>({...m.organizations,membership_role:m.role,access_scope:m.access_scope})).filter(Boolean);const ids=centers.map((c:any)=>c.id);if(!ids.length)return json(req,{centers:[],requests:[],patients:[],appointments:[],members:[],account_role:accountRole});const [rq,pq,aq,mq,asq]=await Promise.all([svc.from("patient_service_requests").select("*").in("assigned_organization_id",ids).order("submitted_at",{ascending:false}).limit(300),svc.from("patients").select("id,organization_id,patient_user_id,external_reference,first_name,last_name,preferred_name,birth_date,email,status,created_at,data_classification").in("organization_id",ids).order("created_at",{ascending:false}).limit(500),svc.from("organization_appointments").select("id,organization_id,patient_id,assigned_user_id,appointment_type,scheduled_start,scheduled_end,status,booking_source").in("organization_id",ids).gte("scheduled_start",new Date(Date.now()-86400000).toISOString()).order("scheduled_start").limit(500),svc.from("organization_members").select("organization_id,user_id,role,status,access_scope").in("organization_id",ids).eq("status","active"),svc.from("patient_care_assignments").select("organization_id,patient_id,professional_user_id,status").in("organization_id",ids).eq("professional_user_id",actor.id).eq("status","active")]);if(rq.error||pq.error||aq.error||mq.error||asq.error)return json(req,{error:"overview_failed",detail:rq.error?.message||pq.error?.message||aq.error?.message||mq.error?.message||asq.error?.message},500);const assignedPatientIds=new Set((asq.data??[]).map((x:any)=>x.patient_id));const visibleReq=(rq.data??[]).filter((r:any)=>isAdmin||managerIds.has(r.assigned_organization_id)||r.assigned_professional_user_id===actor.id);const visiblePatients=(pq.data??[]).filter((p:any)=>isAdmin||managerIds.has(p.organization_id)||assignedPatientIds.has(p.id));const visibleAppts=(aq.data??[]).filter((a:any)=>isAdmin||managerIds.has(a.organization_id)||a.assigned_user_id===actor.id);const visibleMembers=(mq.data??[]).filter((m:any)=>isAdmin||managerIds.has(m.organization_id)||m.user_id===actor.id);const requests=await Promise.all(visibleReq.map(async(r:any)=>({...r,profile:await profile(r.user_id),email:await userEmail(r.user_id)}))),members=await Promise.all(visibleMembers.map(async(m:any)=>({...m,email:await userEmail(m.user_id),profile:await profile(m.user_id)})));return json(req,{centers,requests,patients:visiblePatients,appointments:visibleAppts,members,account_role:accountRole})}
if(action==="create_center"){if(!["professional","admin"].includes(accountRole))return json(req,{error:"professional_required"},403);const name=clean(body.name,180),city=clean(body.city,120)||null,address=clean(body.address_line,240)||null,postal=clean(body.postal_code,24)||null,country=clean(body.country_code,2).toUpperCase()||"FR",timezone=clean(body.timezone,80)||"Europe/Paris",contact=clean(body.contact_email,240)||actor.email||null;if(name.length<2||!/^[A-Z]{2}$/.test(country))return json(req,{error:"invalid_center_fields"},400);let scope="motion";if(isAdmin)scope="clinical";else{const ms=await memberships();if(ms.some((m:any)=>m.access_scope==="clinical"))scope="clinical";else{const ap=await svc.from("professional_applications").select("access_scope").eq("user_id",actor.id).eq("status","approved").order("submitted_at",{ascending:false}).limit(1).maybeSingle();scope=ap.data?.access_scope==="clinical"?"clinical":"motion"}}const slug=`${slugify(name)}-${crypto.randomUUID().slice(0,8)}`,ins=await svc.from("organizations").insert({name,slug,city,address_line:address,postal_code:postal,country_code:country,timezone,status:"active",clinical_data_status:"test_only",booking_published:isAdmin&&body.booking_published===true,contact_email:contact,created_by:actor.id}).select("*").single();if(ins.error)return json(req,{error:"center_create_failed",detail:ins.error.message},500);const center=ins.data,mem=await svc.from("organization_members").insert({organization_id:center.id,user_id:actor.id,role:"owner",status:"active",access_scope:scope,invited_by:isAdmin?actor.id:null,joined_at:new Date().toISOString()});if(mem.error){await svc.from("organizations").delete().eq("id",center.id);return json(req,{error:"center_owner_failed",detail:mem.error.message},500)}const services:any[]=[{organization_id:center.id,service_type:"motion",enabled:true,duration_minutes:30,booking_horizon_days:60,min_notice_hours:12}];if(scope==="clinical")services.push({organization_id:center.id,service_type:"clinical",enabled:true,duration_minutes:30,booking_horizon_days:60,min_notice_hours:12});const sr=await svc.from("organization_booking_services").insert(services);if(sr.error)return json(req,{error:"center_services_failed",detail:sr.error.message},500);const hr=await svc.from("organization_booking_hours").insert([1,2,3,4,5].map(weekday=>({organization_id:center.id,weekday,start_time:"09:00",end_time:"18:00",enabled:true})));if(hr.error)return json(req,{error:"center_hours_failed",detail:hr.error.message},500);await svc.from("audit_events").insert({organization_id:center.id,actor_user_id:actor.id,event_type:"center_created",entity_type:"organization",entity_id:center.id,event_detail:{source:isAdmin?"admin":"professional",access_scope:scope,booking_published:center.booking_published}});return json(req,{ok:true,center,access_scope:scope})}
const oid=clean(body.organization_id,64);if(!oid)return json(req,{error:"organization_required"},400);if(action==="update_center"){if(!(await canManage(oid)))return json(req,{error:"center_manager_required"},403);const patch:any={};for(const [k,max] of [["name",180],["city",120],["address_line",240],["postal_code",24],["contact_email",240],["timezone",80]] as const){if(body[k]!==undefined)patch[k]=clean(body[k],max)||null}if(body.country_code!==undefined){const cc=clean(body.country_code,2).toUpperCase();if(!/^[A-Z]{2}$/.test(cc))return json(req,{error:"invalid_country"},400);patch.country_code=cc}patch.updated_at=new Date().toISOString();const q=await svc.from("organizations").update(patch).eq("id",oid).select("*").single();if(q.error)return json(req,{error:"center_update_failed",detail:q.error.message},500);return json(req,{ok:true,center:q.data})}if(action==="set_publish"){if(!isAdmin)return json(req,{error:"admin_required"},403);const published=body.booking_published===true,q=await svc.from("organizations").update({booking_published:published,updated_at:new Date().toISOString()}).eq("id",oid).select("*").single();if(q.error)return json(req,{error:"publish_failed",detail:q.error.message},500);await svc.from("audit_events").insert({organization_id:oid,actor_user_id:actor.id,event_type:published?"center_published":"center_unpublished",entity_type:"organization",entity_id:oid,event_detail:{booking_published:published}});return json(req,{ok:true,center:q.data})}return json(req,{error:"unknown_action"},400)});