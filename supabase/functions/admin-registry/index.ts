import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";
const U=Deno.env.get("SUPABASE_URL")??"",A=Deno.env.get("SUPABASE_ANON_KEY")??"",S=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";const ORIGINS=new Set(["https://pulse.komolongevity.com","https://komolongevity.com"]);
function cors(req:Request){const origin=req.headers.get("origin")??"";return{"Access-Control-Allow-Origin":ORIGINS.has(origin)?origin:"https://pulse.komolongevity.com","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Vary":"Origin"}}function json(req:Request,body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{...cors(req),"Content-Type":"application/json; charset=utf-8"}})}function num(v:any){const n=Number(v);return Number.isFinite(n)?n:null}function qLevel(score:number|null){if(score===null)return null;const d=100-score;if(d<7)return 0;if(d<16)return 1;if(d<24)return 2;return 3}function tLevel(ratio:number|null){if(ratio===null)return null;if(ratio>=1.3)return 0;if(ratio>=1.1)return 1;if(ratio>=0.9)return 2;return 3}function prep(responses:any){const keys=['baseline','chair_stand','two_step'],done=keys.filter(k=>responses?.[k]?.completed_at).length,q=num(responses?.baseline?.questionnaire?.mobility_score_0_100),chair=num(responses?.chair_stand?.repetitions),two=num(responses?.two_step?.ratio),levels=[qLevel(q),tLevel(two)].filter((x):x is number=>Number.isFinite(x)),freeLevel=levels.length?Math.max(...levels):null;return{completed:done,total:3,percent:Math.round(done/3*100),first_score:q,chair_repetitions:chair,two_step_ratio:two,free_level:done===3?freeLevel:null,free_label:done===3&&freeLevel!==null?['Mobilité préservée','Mobilité à surveiller','Diminution fonctionnelle probable','Diminution fonctionnelle marquée'][freeLevel]:null}}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors(req)});if(req.method!=="POST")return json(req,{error:"method_not_allowed"},405);const token=(req.headers.get("Authorization")??"").replace(/^Bearer\s+/i,"");if(!token)return json(req,{error:"unauthorized"},401);const userClient=createClient(U,A,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false}}),service=createClient(U,S,{auth:{persistSession:false}});const userResult=await userClient.auth.getUser(token),actor=userResult.data?.user;if(userResult.error||!actor)return json(req,{error:"unauthorized"},401);const roleResult=await service.from("account_roles").select("role").eq("user_id",actor.id).maybeSingle();if(roleResult.data?.role!=="admin")return json(req,{error:"admin_required"},403);let body:any={};try{body=await req.json()}catch{return json(req,{error:"invalid_json"},400)}if(String(body.action??"list")!=="list")return json(req,{error:"unknown_action"},400);
const [rolesResult,applicationsResult,usersResult]=await Promise.all([service.from("account_roles").select("user_id,role").eq("role","member").limit(1000),service.from("professional_applications").select("user_id,status").limit(1000),service.auth.admin.listUsers({page:1,perPage:1000})]);if(rolesResult.error)return json(req,{error:"roles_failed",detail:rolesResult.error.message},500);if(applicationsResult.error)return json(req,{error:"applications_failed",detail:applicationsResult.error.message},500);if(usersResult.error)return json(req,{error:"users_failed",detail:usersResult.error.message},500);const memberIds=(rolesResult.data??[]).map((x:any)=>String(x.user_id));if(!memberIds.length)return json(req,{patients:[],counts:{patients:0,member_accounts:0,with_professional_application:0}});
const [profilesResult,recordsResult,requestsResult,freeResult]=await Promise.all([service.from("profiles").select("id,display_name,first_name,last_name,phone,birth_date,sex_at_birth,city,country,locale,newsletter_opt_in").in("id",memberIds),service.from("patients").select("id,patient_user_id,organization_id,status,created_at,external_reference").in("patient_user_id",memberIds).order("created_at",{ascending:false}).limit(2000),service.from("patient_service_requests").select("id,user_id,status,service,submitted_at,assigned_organization_id,assigned_professional_user_id,patient_id,assessment_id,scheduled_at").in("user_id",memberIds).order("submitted_at",{ascending:false}).limit(2000),service.from("pulse_assessments").select("id,user_id,status,responses,updated_at,completed_at").in("user_id",memberIds).eq("protocol_version","mobility-check-v1").order("updated_at",{ascending:false})]);if(profilesResult.error)return json(req,{error:"profiles_failed",detail:profilesResult.error.message},500);if(recordsResult.error)return json(req,{error:"patient_records_failed",detail:recordsResult.error.message},500);if(requestsResult.error)return json(req,{error:"patient_requests_failed",detail:requestsResult.error.message},500);if(freeResult.error)return json(req,{error:"free_results_failed",detail:freeResult.error.message},500);
const profileMap=new Map((profilesResult.data??[]).map((p:any)=>[String(p.id),p])),appMap=new Map<string,any[]>(),recordMap=new Map<string,any[]>(),requestMap=new Map<string,any[]>(),freeMap=new Map<string,any>();for(const a of applicationsResult.data??[]){const id=String((a as any).user_id),arr=appMap.get(id)??[];arr.push(a);appMap.set(id,arr)}for(const r of recordsResult.data??[]){const id=String((r as any).patient_user_id),arr=recordMap.get(id)??[];arr.push(r);recordMap.set(id,arr)}for(const r of requestsResult.data??[]){const id=String((r as any).user_id),arr=requestMap.get(id)??[];arr.push(r);requestMap.set(id,arr)}for(const f of freeResult.data??[]){const id=String((f as any).user_id);if(!freeMap.has(id))freeMap.set(id,f)}const members=new Set(memberIds);const patients=(usersResult.data?.users??[]).filter((u:any)=>members.has(String(u.id))).map((u:any)=>{const applications=appMap.get(String(u.id))??[],records=recordMap.get(String(u.id))??[],requests=requestMap.get(String(u.id))??[],free=freeMap.get(String(u.id))??null;return{user_id:u.id,email:u.email??null,created_at:u.created_at??null,last_sign_in_at:u.last_sign_in_at??null,email_confirmed_at:u.email_confirmed_at??null,profile:profileMap.get(String(u.id))??null,patient_records:records,patient_record_count:records.length,service_requests:requests,service_request_count:requests.length,free_preparation:prep(free?.responses||{}),free_status:free?.status||'not_started',free_updated_at:free?.updated_at||null,free_completed_at:free?.completed_at||null,has_professional_application:applications.length>0,professional_application_statuses:applications.map((a:any)=>a.status)}}).sort((a:any,b:any)=>String(b.created_at??"").localeCompare(String(a.created_at??"")));const patientOnly=patients.filter((p:any)=>!p.has_professional_application);return json(req,{patients:patientOnly,counts:{patients:patientOnly.length,member_accounts:patients.length,with_professional_application:patients.length-patientOnly.length}})});
